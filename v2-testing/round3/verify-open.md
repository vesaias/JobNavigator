# OPEN-list fix verification (round 3, open items)

Branch `v2-redesign`, started at HEAD `2927bdb` ("Settings value validation (int/cron/enum) + scheduler boot resilience; snap-to-pixel on the remaining modals; run summary counts filtered rows like ScrapeLog; URL reader decodes entities; 15 silent catches → toasts; Schedules card failure state; Escape via useEscape on Feed/Applications; one-click score honours scoring_default_depth; save-from-extension reports the exclusion reason; preview rewrites tracked links; Jobright session read-only with expiry + sign out"), finished after a mid-session rebuild landed HEAD `63790cf` ("Health: inactive searches/companies never count as needing attention; per-entity acknowledge (columns, endpoints, row links); muted last-run notes on paused/acknowledged rows") — fresh browser contexts were opened for every item run after that rebuild. Backend container Playwright vs `http://caddy`, API key `pick-a-password`. Scripts: local `scratchpad/i*.py` copied to `backend:/tmp/v2t/` via `docker compose cp`, run via `docker compose exec -T backend python /tmp/v2t/<name>.py`. `DOCKER="/c/Program Files/Docker/Docker/resources/bin/docker.exe"; export MSYS_NO_PATHCONV=1`. Scratch rows prefixed `ZZV`, swept at the end (jobs via scoped SQL — no `DELETE /jobs/{id}` endpoint). No commits/rebuilds/restarts/source edits made by this session. No LLM calls made — scoring intercepted via Playwright route stubs on `**/api/analyze*` fulfilling 202.

Read first: `v2-testing/round3/verify-final.md` (method + harness gotchas: `route.fulfill()` timing — never `time.sleep()` before fulfilling, hold the route object instead; `text=` locator ambiguity — prefer `get_by_text(exact=True)` or scoped XPath), `v2-testing/REPORT-open.md` rows OPEN-01..06/08/10/12/13/16, and `git show --stat` + the full diff for every touched file in both `2927bdb` and `63790cf` (the latter added mid-session, covering the **HEALTH-ACK** item), read before exercising each item live.

## Results

| id | status | measured |
|---|---|---|
| HEALTH-ACK | ✔ | Commit `63790cf` ("Health: inactive searches/companies never count as needing attention; per-entity acknowledge…"). **(a)** No existing warning in `/health/entities` (empty), so created `ZZV Warn` (`sources:["zip_recruiter"]`), ran it for real → genuine `403` (`source_breakdown:{"zip_recruiter":{"error":"403"}}`) → listed: `{"id":…,"reason":"ZipRecruiter failed (403) on the last run"}`. UI before pause: Searches header `"7 configs · 5 active · 1 need attention ·…"`, row shows the warning + an `Acknowledge` link, rail dot title `"0 companies and 1 search need attention. Click → Stats · Run history."`. `PATCH active:false` → gone from `/health/entities` (`searches:[]`); header text no longer contains "need attention" (null); row now reads `"ZipRecruiter failed (403) on the last run · paused"` with a `Paused` badge, muted, no `Acknowledge` link. `PATCH active:true` → listed again. **(b)** `POST /searches/{id}/acknowledge` → 200 `{"warning_acknowledged_at":"2026-09-03T12:56:29…"}`; immediately gone from `/health/entities`; row reads `"… · acknowledged just now"`, still `Active` (not paused). Ran it again (still failing) → listed again (API path). Clicked the UI `Acknowledge` link live on the re-raised row → toast `"“ZZV Warn” acknowledged — it stops counting until a later run fails"`, row text updated to `… · acknowledged just now`, gone from `/health/entities` again. Ran it a third time → listed again (newer failed run re-raises it, both the API-driven and UI-driven acknowledgements re-raise identically). **(c)** `POST /searches/{Extension LI id}/acknowledge` → 409 `"Extension searches have no scrape health to acknowledge — jobs arrive from the browser extension"`; unknown id → 404 `"Search not found"`. **(d)** No company had a warning either; created `ZZV Dead Co` (`scrape_urls:["https://job-boards.greenhouse.io/this-company-definitely-does-not-exist-zzv"]`), ran it 3 times for real (Greenhouse 404s the nonexistent slug — no hard per-run error, just 0 results each time, so it took all 3 of the `window=3` runs to qualify, not the single-run-error shortcut `ZZV Warn` hit) → listed: `{"reason":"No results in the last 3 scrapes"}`. Unknown company id → `POST /companies/{id}/acknowledge` → 404 `"Company not found"`. Drawer opened on `ZZV Dead Co`: banner present (`"No results in the last 3 scrapes…"`) with an `Acknowledge` link (count 1). `POST /companies/{id}/acknowledge` → 200; gone from `/health/entities`; drawer re-opened → `Acknowledge` link gone (count 0), row/banner text now reads `"last run found nothing · never · acknowledged just now"` (the muted fallback text differs from the pre-ack `/health/entities`-sourced reason because `downMap[id]` itself disappeared once acknowledged — `warnTextOf` fell back to the company's own `last_run_warning` flag, still muted/no-link either way). Cleanup: `ZZV Warn` search deleted (`DELETE /searches/{id}` → 200, 0 jobs — all runs failed with 0 results), `ZZV Dead Co` company deleted (`DELETE /companies/{id}` → 200, 0 jobs). Final `/health/entities` back to `{"companies":[],"searches":[],"count":0}`. |
| OPEN-16 | ✔ | Settings › Accounts › Jobright › **Session** row (`RO(...)` readonly kind). Confirmed a live session is present (`jobright_session_id` GET-redacted to `••••••`, never cleared/PATCHed — the real value can't be restored once cleared, so it was left untouched throughout). Only `jobright_session_obtained_at` (freely reversible metadata, originally `""`) was PATCHed to walk the 3 reachable display states — the fourth ("Not signed in…") was confirmed by code inspection only, since forcing it live would mean clearing the real `jobright_session_id`, which the item's own guard forbids. **Current live state** (`obtained_at=""`): row reads `••••••` / `"session stored — expiry unknown"` / `Sign out`. **`obtained_at=now`**: `••••••` / `"session valid until 11/2/2026"` (60 days out) / `Sign out`. **`obtained_at=65 days ago`**: `••••••` / `"expired 8/29/2026 — renewed on the next run"` / `Sign out`. **Not-signed-in** (code-inspected, not forced live): `Settings.jsx`'s `readonly` case renders `r.emptyText` (`"Not signed in — the next Jobright run signs in for you."`) whenever `!val(r.key)`, matching the row's `emptyText` prop. Sign out control (`ActionBtn` labelled "Sign out") present in all 3 live-checked states; **not clicked**, since `jobright_session_id` was non-empty throughout (per the item's own guard). Restored `jobright_session_obtained_at` to `""` afterward, confirmed via `GET /settings`. |
| OPEN-13 | ✔ | `tracer_links_enabled=true`, `tracer_links_base_url=https://viktoresadze.com`, `tracer_links_url_style=param_jobid` (all read, unmodified). Picked base résumé `PjM FinTech` (`6d2ca1a7-…`) with 2 linked contact items (`linkedin.com/in/viktoresadze`, `viktoresadze.com`) plus a `mailto:` email. `TracerLink` rows for this résumé before: **2**. `GET /resumes/{id}/preview` (HTML) → both contact `href`s render as `https://viktoresadze.com?cv=u17eoi` / `…?cv=j6vozw` — tracked param-style URLs, not the raw destinations (confirmed by exact-match check against the raw destination strings, not substring — a naive substring check false-positives here because the tracer base domain and the personal-site destination are the same domain). The `mailto:` link is untouched (not tracer-rewritten, expected — only http(s) contact links go through `_rewrite_urls_with_tracers`). `TracerLink` count after preview: **2** (unchanged — reused the existing per-(owner,destination) links, minted none). Then `GET /resumes/{id}/pdf` (real Playwright render) → 200; `TracerLink` count after: **2** (still unchanged). Preview and PDF agree on the same tokens, and neither endpoint leaks a raw destination URL. |
| OPEN-12 | ✔ | Read `title_exclude_global` (80 keywords, includes `developer`). `POST /jobs/save-from-extension` with title `"ZZV Senior Developer Role"` → `{"saved": false, "status": "ignored", "reason": "title excluded by 'developer'"}` — reason names the exact matched keyword. Second call, title `"ZZV Program Coordinator Role"` (hits none of the global or per-search exclude keywords) → `{"saved": true, "status": "new"}`. Both jobs deleted by id via SQL. |
| OPEN-10 | ✔ | Read `scoring_default_depth` (`"light"`, unchanged from the default). Created one throwaway unscored job (`POST /jobs/save-from-extension`, `ZZV Depth Test Job` @ `ZZV Depth Co`, generic description — every real job already in this DB is scored, so the row `SCORE` pill never renders otherwise) and isolated it via the Feed's title-search box. Stubbed `**/api/analyze/*` to fulfill 202 immediately (no LLM call) and captured the request URL. **Light pass** (`scoring_default_depth="light"`): pill tooltip = `"Score this role — light (score only). Change the default in Settings › Scoring, or press r to pick."`; clicking it posted `…/api/analyze/{id}?depth=light`; rescore modal (row ⋯ → Rescore) opened with **Light** highlighted (`background: rgb(234,241,235)` = `--accent-soft`, `border-color: rgb(63,107,82)` = `--accent`) and **Full** at rest. **Full pass** (`PATCH scoring_default_depth="full"`): pill tooltip = `"Score this role — full (score + keywords + report)"`; posted `…/api/analyze/{id}?depth=full`; rescore modal opened with **Full** highlighted, **Light** at rest. Restored `scoring_default_depth` to `"light"` afterward, confirmed via `GET /settings`. Test job deleted by SQL id; confirmed no `ZZV` company was auto-created (save-from-extension doesn't create companies, per architecture). |
| OPEN-08 | ✔ | **Feed**: rescore modal (row ⋯ → Rescore) open → `zIndex:60` overlay present → Escape → overlay gone. Detail panel opened (`?job=`), its head-menu → "Ignore … everywhere" → ConfirmDialog (`zIndex:70`) opens with the detail panel still behind it (`window.location.search` still carries `job=`) → Escape → `zIndex:70` overlay gone, `zIndex:60`/detail state unaffected — `location.search` still carries `job=` after, confirming only the dialog closed (matches `useEscape(onCancel)` inside `ConfirmDialog` claiming the event via `e.preventDefault()` before the screen-level hook — gated `!confirm` — ever sees it). Company filter dropdown opened (its "Type to search N companies…" input is `autoFocus`, confirmed `document.activeElement.tagName === "INPUT"`), typed `zzv-probe` into it, dropdown content (`"Type to search"` in the DOM) present → Escape → dropdown content gone — FEED-16: no `INPUT`/`TEXTAREA`/`SELECT` tag guard on the Escape branch, so it closes the menu even while the cursor sits in the menu's own search box. **Applications**: Log modal open, typed `ZZV escape test` into the Title field (making it dirty) → Escape → `ConfirmDialog` (`zIndex:70`, "Discard this application?") appeared, Log modal (`zIndex:60`) still present underneath → Escape again → discard dialog gone, Log modal still open (matches `Applications.jsx`'s screen-level `useEscape(..., !confirm)` gating and `ConfirmDialog`'s own always-active `useEscape`). All 5 Escape behaviors from the item confirmed exactly as the diff describes. |
| OPEN-06 | ✔ | Stubbed `GET **/api/scheduler/jobs*` (confirmed exact path: `backend/main.py:799 @app.get("/api/scheduler/jobs")`) → 500, opened `/v2/stats`. The Schedules card's full text: `"Schedules\nintervals and crons live in Settings\nUnavailable — the request failed"` — the row-table header/rows are gone and replaced with the failure branch, matching `Stats.jsx`'s `schedErr` state (`setJobs([]); setSchedErr(true)` on a failed `Promise.allSettled`/`.catch(() => null)`) and its `{schedErr ? <div>Unavailable — the request failed</div> : (...)}` render. Exact string match. |
| OPEN-05 | ✔ | Stubbed `GET **/api/resumes*` (matching `is_base=true`) → 500, opened Applications' Log modal (`+ Log application`) → error toast appeared: `"Could not load your résumés — log it now and attach one later."` (matches `Applications.jsx`'s converted catch exactly). Stubbed `GET **/api/settings*` → 500 and opened `/v2/cover-letters` (the list page's own voice-preset source, since it has no dedicated `/cover-letters/templates` call) → toast `"Could not load the voice presets — the voice picker is empty."` (matches `CoverLetters.jsx`). Extra coverage: stubbed `GET **/api/cover-letters/templates*` → 500 on the **editor** route `/v2/cover-letters/{id}` (an existing letter, `ce44e1d4-…`) → toast `"Could not load the layouts — the picker is empty."` (matches `CoverLetterEditor.jsx`, the literal endpoint named in the item). All three toasts non-empty, user-visible, and byte-match the diff's converted `.catch` messages — none of these three failure paths are silent any more. |
| OPEN-04 | ✔ | `POST /applications/extract` (real fetch, no stub). **LinkedIn**: the two numeric-ID `linkedin.com/jobs/view/{id}` URLs pulled from the DB (both from job records) resolved to LinkedIn's generic "Freshworks jobs in United States" landing page, not the posting — those numeric-only IDs are stale/expired and LinkedIn redirects an unauthenticated fetch to a jobs-search page (not a defect in the reader; a URL-liveness artifact). Found the actual repro from the OPEN-04 finding's own DB record — LinkedIn's slug-form URL `…/lead-technical-product-manager-ai-strategy-health-plan-tech-remote-kansas-at-optum-4460587357` (still live, discovered 2026-08-30) → `{"title":"Lead Technical Product Manager - AI Strategy & Health Plan Tech - Remote Kansas","company":"Optum"}` — a raw `&`, no `&amp;` anywhere in title or company (this is the exact "…AI Strategy &amp; Health Plan Tech…" case the OPEN-04 finding names). **Greenhouse**: `https://job-boards.greenhouse.io/celonis/jobs/6313858003?gh_jid=6313858003` → `{"title":"Head of Industry - Retail & CPG","company":"Celonis"}` — raw `&`, no `&amp;`. Both confirm `_decode_entities()` (`html.unescape`, up to 3 passes) is applied to the JSON-LD-sourced title/company before the response is built. |
| OPEN-03 | ✔ | Created `ZZV Sum` keyword search (`sources:["indeed"]`, `search_term:"software engineer"`, `results_wanted:6`, `title_exclude_keywords:["senior"]`, `auto_scoring_depth:"off"`), triggered a real run (`POST /searches/{id}/run` → 202, polled `/monitor/run/{run_id}` to `completed`, no scoring — `auto_scoring_depth:"off"` and no stub needed). Three-way reconciliation: `JobRun.result_summary` (both `/monitor/run/{id}` and `/monitor/history`, byte-identical) = `"ZZV Sum - 3 seen, +3 new, 2 filtered out"`; `ScrapeLog` row read directly (`jobs_found:3, new_jobs:3, source_breakdown:{"indeed":{"new":3,"seen":3,"filtered":2}}`, `is_warning:false`) — the summary's `3 seen, +3 new` matches the log exactly, and its `2 filtered out` matches `source_breakdown.indeed.filtered`. `jobs` rows with `search_id` = that search: 5 total, `by_status:{"new":3,"ignored":2}` — `3 kept (new) + 2 filtered (ignored) = 5 stored rows`, matching `found(3) + filtered(2)`. All three views agree; the accounting gap the OPEN-03 finding described ("8 rows stored vs 6 seen, +6 new with nothing to explain the difference") is now explained end-to-end by the `filtered` counter. Cleanup: `DELETE FROM jobs WHERE search_id=...` (5 rows) + `DELETE /searches/{id}` → 200. (Note: an earlier run of this same script died on an unrelated script bug — a leftover `TypeError` from a stray duplicate `get()` call — after a real run had already completed and stored 5 job rows under a first `ZZV Sum` search; that search + its 5 jobs were found and cleaned up by SQL/`DELETE /searches` before the corrected re-run below, both accounted for in the final ZZV sweep.) |
| OPEN-02 | ✔ | Measured `getBoundingClientRect().top` on each modal's panel (the `useSnapTop`-refed element) at 1440×900 and 1440×901, both fresh browser contexts per height: **Applications Log modal** (`+ Log application`) `top=191`/`192`; **Applications Prep modal** (row → "Builds one pasteable block" prep button) `top=130`/`131`; **Feed rescore modal** (row ⋯ → Rescore) `top=165`/`166`; **Feed picker modal** (row ⋯ → Tailor résumé) `top=137`/`138`; **WelcomeModal** (forced via `sessionStorage.setItem('jn:welcome','1')`, the actual gate read in `App.jsx:110-111`) `top=220`/`221`; **LoginModal** (fresh context, `localStorage.removeItem('jobnavigator_api_key')`, `/v2` → 401 on `set-session` → `showLogin`) `top=306`/`306`. All 12 values (6 modals × 2 heights) integers — `useSnapTop` confirmed wired into all six, matching the diff (`Applications.jsx` `PrepModal`/`LogModal`, `JobFeed.jsx` `pickerPanel`/`rescorePanel`, `WelcomeModal.jsx`, `LoginModal.jsx`). |
| OPEN-01 | ✔ | Read originals first (`scrape_interval_minutes=3500`, `backup_cron="0 3 * * *"`, `llm_provider="claude_code"`). `PATCH /settings {scrape_interval_minutes:"abc"}` → 400 `"Invalid setting value — scrape_interval_minutes: must be a whole number (got 'abc')"`. `{backup_cron:"0 4 * *"}` (4 fields) → 400 `"...needs exactly 5 whitespace-separated fields (min hour day month dow)"`. `{backup_cron:"x y z w v"}` (5 garbage fields) → 400 `"...not a valid cron expression (Invalid month name \"w\")"`. `{llm_provider:"nope"}` → 400 `"...must be one of claude_api, claude_code, ollama, openai, openrouter (got 'nope')"`. Valid values (`scrape_interval_minutes:"45"`, `backup_cron:"0 3 * * *"`, `llm_provider:"claude_api"`) → 200 each. Multi-bad in one PATCH (`scrape_interval_minutes:"xyz"`, `email_check_interval_minutes:"-5"`, `backup_cron:"bad cron here"`) → single 400 listing all three: `"...backup_cron: needs exactly 5...; email_check_interval_minutes: must not be negative (got '-5'); scrape_interval_minutes: must be a whole number (got 'xyz')"`. **UI**: on `/v2/settings`, typed `abc` into the "Scrape all companies" integer box (`scrape_interval_minutes`) — SET-27's client-side digit filter strips non-digits as typed, so the field held `""` at blur; `PATCH` fired with `scrape_interval_minutes:""` → 400, and the on-screen message read exactly `"Invalid setting value — scrape_interval_minutes: must be a whole number (got '')"` — the server detail, not the old generic `"Could not save — try again"` (confirms `Settings.jsx`'s `catch` branch reading `e.response.data.detail` on a 400). Field reverted to its prior value `3500` on screen (SET-08 rollback), matching the server (never actually changed — PATCH was rejected). All three touched keys restored to their original values and confirmed via a follow-up `GET /settings` (`restored_match: {all true}`). |

## Summary

**12 of 12 items ✔ (fully verified working live)**: OPEN-01, 02, 03, 04, 05, 06, 08, 10, 12, 13, 16, and HEALTH-ACK (a)-(d) all confirmed exactly as their commits (`2927bdb`, `63790cf`) describe — server-side value validation with UI surfacing, all 6 remaining modals snap to the pixel grid, the run summary reconciles byte-for-byte with the ScrapeLog row and the stored job rows, the URL reader decodes HTML entities on both a live repro and a fresh Greenhouse fetch, three previously-silent `.catch` sites now show toasts with the exact converted message, the Stats Schedules card has an explicit failure state, Escape is fully migrated to the shared `useEscape` hook on both Feed and Applications (including the ConfirmDialog-only-closes-itself and FEED-16 no-INPUT-guard cases), the one-click SCORE pill and its tooltip honour `scoring_default_depth` while the rescore modal preselects it, save-from-extension names the exact exclusion keyword, `/resumes/{id}/preview` now rewrites tracked links identically to `/pdf` without minting duplicate `TracerLink` rows, the Jobright Session row shows the correct masked-value/expiry state for every reachable case, and inactive/paused/acknowledged entities are fully excluded from `/health/entities`, the header counts, and the rail dot while staying visible (muted) on their own row/drawer, acknowledge is scoped per-entity with proper 409/404 guards, and a newer failed run reliably re-raises an acknowledged warning.

No defects found in this pass — every item in scope for this round was previously fixed and reverified working.

## ZZV sweep

Final sweep (`i99_sweep.py`) queried `jobs` (by `title`/`company`), `companies`, `searches`, `resumes`, `cover_letters` (joined to `jobs`), `applications` (joined to `jobs`), `activity_log_v2`, and `tracer_links` (by `source_label`) for any row matching `%ZZV%`. Found and removed 2 leftover `activity_log_v2` rows (real log lines from OPEN-03's `ZZV Sum` search runs) that the per-item cleanup had not targeted; everything else was already clean from per-item cleanup (jobs via scoped SQL delete — no `DELETE /jobs/{id}` endpoint — companies/searches/resumes via their DELETE endpoints). **Final state: 0 `ZZV` rows remaining anywhere.**

## Settings restore

Every setting value touched across the full session — `scrape_interval_minutes` (3500), `backup_cron` ("0 3 * * *"), `llm_provider` ("claude_code"), `scoring_default_depth` ("light"), `jobright_session_obtained_at` ("") — read back via a final `GET /settings` and confirmed to match their original values exactly (`all_match: true`, 0 mismatches). `jobright_session_id` itself was never touched (its real value is write-only/redacted by `GET`, so it was left alone throughout, per the item's own guard against clicking Sign out on a live session). Final `GET /health/entities` reads `{"companies":[],"searches":[],"count":0}` — no test warnings left registered.
