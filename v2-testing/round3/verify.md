# Round-3 fix verification (R3-3 → R3-4 handoff)

Branch `v2-redesign` @ HEAD `ec33983` (`8804ae3` "Round-3 fixes: …" + a test-marker commit). Backend container Playwright vs `http://caddy`, API key `pick-a-password`. Scripts: `scratchpad/r3v_*.py` copied to `backend:/tmp/v2t/`. Scratch rows prefixed `ZZV`, swept at the end (jobs via scoped SQL — no `DELETE /jobs/{id}` endpoint, same precedent as prior rounds).

Read first: `v2-testing/ROUND3-PLAN.md` (R3-3), `v2-testing/round2/verify.md` (method + gotchas), `v2-testing/round3/flows-A.md` (R3-A-02/05/08), `flows-B.md` (R3-B-02/03/04/05/06), `smoke.md` (R3-S-01..04), `user-notes.md` (R3-U-01/02). Every item was also checked by reading the `8804ae3` diff for the relevant file(s) before exercising it live, to confirm the live behaviour matches the fix's own stated mechanism, not just its symptom.

## Results

| id | status | measured |
|---|---|---|
| 1. R3-A-08 (P1) | ✔ | Created `ZZV Del Co` (Greenhouse `boards.greenhouse.io/duolingo`) → `POST /api/scrape/company/{id}` real scrape (61 found/55 new) → `GET /scrape-log` confirmed a row `{source:"playwright_ZZV Del Co", company_id:<id>, is_warning:false}`. Companies UI: search box → row → `⋯` → `Delete company` → `ConfirmDialog` (`Delete ZZV Del Co?` / `Jobs already found are kept.`) → clicked `Delete` → `DELETE /api/companies/{id}` → **200**, toast `ZZV Del Co deleted` (count 1), **0** error toasts, row gone, console/pageerrors/http≥400 all 0. Created `ZZV Del Search` (keyword, `auto_scoring_depth:"off"`, `results_wanted:5`) → `POST /searches/{id}/run` real run (5 seen/5 new) → Searches UI → `⋯` → `Delete search` → confirm dialog opened (`Delete "ZZV Del Search"?`) → `Delete` → `DELETE /api/searches/{id}` → **200**, card gone, 0 error toasts. Verified one of the search's jobs via `GET /jobs/{id}` afterward: `search_id: null` (was the search's id before delete) — matches the `db.query(Job).filter(...).update({"search_id": None})` in `routes_searches.py`'s `delete_search`. Both deletes matched the `8804ae3` diff exactly (orphan ScrapeLog/Job rows before `db.delete`, catch `IntegrityError` → 409 fallback never hit because the orphaning succeeded). Cleanup: 88 leftover `company ILIKE 'ZZV%'` jobs (the Duolingo scrape's 55 plus pre-existing `ZZV%`-company rows from earlier passes) + the search's 5 orphaned jobs deleted by SQL (companies/searches themselves already gone via the UI deletes). |
| 2. R3-A-02 (P1) | ✔ | `GET /api/settings` → `dedup_tracking_params` **does not contain `jk`** (confirmed the full list). Ran `ZZV Indeed` (keyword `"product manager"`, `sources:["indeed"]`, `results_wanted:10`, real JobSpy) → `GET /api/jobs?source=jobspy_indeed&limit=20` returned **8** rows, all created within the same 2-second window (`discovered_at` 09:14:50–09:14:52), all with **distinct** `external_id`s (8/8 unique) and all tagged with this search's `search_id`. Run summary (`/monitor/history`): `"ZZV Indeed - 6 seen, +6 new"` and `ScrapeLog` `{jobs_found:6, new_jobs:6}` — 2 short of the 8 stored rows; not investigated further since it's an accounting nuance in the run-summary count, not the dedup-collision defect under test (the defect's signature — one shared `external_id` swallowing every posting — is definitively absent: 8 distinct ids, 8 distinct postings, including two same-title/company Amazon postings that still hashed to different ids, i.e. two real openings, not a collision). Matches `8804ae3`'s `_IDENTITY_PARAMS = {"indeed.com": {"jk","vjk"}, ...}` guard in `dedup.py` plus the `jk`-stripping `migrate_dedup_tracking_params` seed migration. Cleanup: 8 jobs deleted by SQL (`search_id = '<ZZV Indeed id>'`); search itself deleted via `DELETE /searches/{id}`. |
| 3. R3-A-05 (P2) | ✔ | `POST /api/applications/extract` (real fetches): Greenhouse `job-boards.greenhouse.io/anthropic/jobs/5391227008` → `{title:"AWS GTM Partnership Lead, Public Sector & Nonprofits", company:"Anthropic"}`; Lever `jobs.lever.co/moonpay/91e69ea7-…` → `{title:"Senior Product Manager, KYB - MoonPay Enterprise", company:"MoonPay"}`; Ashby `jobs.ashbyhq.com/baseten/40bce839-…` → `{title:"Technical Program Manager, Model Performance", company:"Baseten"}`; LinkedIn `linkedin.com/jobs/view/lead-technical-product-manager-…-4460587357` → `{title:"Lead Technical Product Manager - AI Strategy &amp; Health Plan Tech - Remote Kansas", company:"Optum"}`. **All four return the real employer, none returns `Greenhouse`/`Lever`/`Ashby`/`Linkedin`** — matches the `8804ae3` fix exactly: `_company_from_url()` reads the board-slug from the path via the same `scraper/ats/*` `is_*`/slug helpers the scrapers use, ahead of the `og:site_name`/hostname fallback, and `_is_ats_brand()` now blocklists the brand-fallback path too. (Minor, out of scope: the LinkedIn title carries a raw `&amp;` HTML entity — not a brand leak, not mentioned in the finding, not touched.) |
| 4. R3-B-03 (P2) | ✔ | Résumé `3c0ef3dd…` ("PM → Runpod — Senior Product Manager", tailored copy) + its cover letter `de0add32…` (found by joining `GET /cover-letters` on `resume_id`). Sequence: **résumé PDF** (200, 105902 bytes) → **cover-letter PDF** (200, 119567 bytes) → **résumé PDF again** (200, 105902 bytes). After **every single one** of the three renders, both `GET /resumes/{id}/tracer-stats` and `GET /cover-letters/{id}/tracer-stats` returned the **same 2 non-empty rows** (`13374d`→viktoresadze.com, `13374l`→LinkedIn) — neither endpoint ever went to `[]`, which is exactly the round-3 defect (whichever document rendered second used to own the link and the other reported empty). Matches the `8804ae3` fix: `_repoint()` in `routes_resumes.py` no longer nulls the other owner's FK, so a shared token keeps both `resume_id` and `cover_letter_id` set and each stats endpoint (which filters on its own FK) reports it. |
| 5. R3-B-05 (P3) | ✔ | Created `ZZV Base` (`POST /resumes`, `is_base:true`) → created `ZZV R3B05 Job`/`ZZV R3B05 Co` via `POST /applications` (manual-log path, only way to create a Job through the API) → created a tailored copy via the **non-LLM** `POST /resumes/copy {base_resume_id, job_id}` (exact copy, no tailoring needed for this test). Set `cv_scores = {"ZZV Base":40, "Tailored":55}, best_cv="Tailored", best_cv_score=55` on the job by SQL (no PATCH field for `cv_scores` — `PATCH /jobs/{id}` only allows `{seen,saved,status}`, confirmed by reading `routes_jobs.py:549`). `DELETE /resumes/{copy_id}` → **200** `{"deleted":true,"children_deleted":0}`. `GET /jobs/{job_id}` afterward: `cv_scores = {"ZZV Base":40}` (**`Tailored` key gone**), `best_cv = "ZZV Base"` (**recomputed**, was `"Tailored"`), `best_score = 40` (the API's live-computed field — `best_cv_score` isn't itself serialized in the job JSON, but the underlying column is what `best_score` is derived from, and 40 is exactly `max()` of what's left, confirming the column was updated too). Matches the `8804ae3` fix: `delete_resume()` now calls `_clear_orphan_tailored_score(db, job_id)` after flushing the delete, which pops `Tailored` from `cv_scores`/`scoring_report` only when no tailored copy remains for the job and recomputes `best_cv`/`best_cv_score` from what's left. Cleanup: application, job, and base resume deleted by SQL (the tailored copy was already gone via the API delete under test). **Leftover found and cleaned up in the final sweep**: this flow's manual-log `POST /applications` call also auto-created a Company row (`ZZV R3B05 Co`) which the per-item cleanup script missed — caught by the final sweep, deleted via `DELETE /companies/{id}`. |
| 6. R3-B-04 (P3) | ✔ | Called `backend.api.routes_autofill._trim_to_chars` directly inside the container (no LLM call, no route stub needed — the helper takes plain text). **714-char, sentence-punctuated text, `max_chars=600`** → output **577 chars** (≤600), `trimmed=True`, cut lands on a sentence boundary (`"…without fail here."`, ends in `.`). **714-char text with no punctuation, `max_chars=600`** → output **599 chars** (≤600), `trimmed=True`, cut lands on a clean word boundary (`"…labore lorem ipsum"`, no partial word). **No-op case** (`'short text'`, 600) → unchanged, `trimmed=False`. **Edge case** (`max_chars=0`) → unchanged, `trimmed=False` (the `max_chars <= 0` guard). Response-flag semantics confirmed by reading `routes_autofill.py`'s `autofill_answer`: it now returns `{"answer": <trimmed>, "trimmed": <bool>, "max_chars": <int>}` instead of just `{"answer"}` — matches the `8804ae3` diff exactly (server-side enforcement, independent of the extension's `fieldMax`-only cap this finding was raised against). |
| 7. R3-B-06 (P4) | ✔ | `/v2/resumes/22ce0e5b…` (base résumé "PM") → `⋯` → the destructive menu item (`.v2-hover-bad`) reads **`✕ Delete résumé`**. `/v2/resumes/3c0ef3dd…` (tailored copy "PM → Runpod — Senior Product Manager") → `⋯` (this branch's trigger has no `title` attribute, unlike the base's `title="More"` — located by `.v2-act:has-text("⋯")` instead) → destructive item reads **`✕ Delete copy`**. Matches the `8804ae3` diff: only the base branch's label text changed (`Delete copy` → `Delete résumé` at `ResumeEditor.jsx`), the copy branch's own menu item was untouched. |
| 8. R3-B-02 (P3) | ✔ | Existing tailored copy `9861bc06…` ("PM → Google — Product Manager II, Search") has `experience[].suggested_bullets` in its `json_data` (found by scanning the DB — 332 tailored copies carry them, no stub needed). `/v2/resumes/{id}` → `⋯ → Review changes` → modal shows **8** rows chipped `suggested` (dashed-border chip, `cursor:help` tooltip "Suggested — not in the document or the PDF yet…") alongside the `applied` rows, header reads "Tailoring changes" (not "— already applied", since `nSuggested>0`), and the footer reads exactly **`14 applied · 6 suggested — added on Done reviewing`** — matches the `N applied · M suggested …` pattern from the task and the `8804ae3` diff's `${liveApplied} applied · ${liveSuggested} suggested — added on Done reviewing…` template. Read-only: did not click Decline or Done reviewing, no mutation. |
| 9. R3-S-03/04 (P4) | ✔ | **Résumé editor head menu (R3-S-03):** base (`22ce0e5b…`) → `⋯` opens menu (`Delete résumé` visible) → `Escape` → menu closed. Tailored copy (`3c0ef3dd…`) → `⋯` opens menu (`Delete copy` visible) → `Escape` → menu closed. Both confirmed both ways (open-before/closed-after), console clean. Matches `8804ae3`'s unconditional `useEscape(() => setHeadMenu(false), headMenu)` added to `ResumeEditor.jsx` (fires for both branches since `headMenu` state is shared). **Settings Model catalog (R3-S-04):** opened `Manage…` → typed `gpt` into the catalog search → live suggestion dropdown appeared (`div[role="listbox"]`, 1). **First Escape**: dropdown gone (0), modal still open (`[aria-label="Close the model catalog"]` still present, count 1). **Second Escape**: modal closed (close-button count 0). Matches the `8804ae3` diff: the input's own `onKeyDown` calls `e.preventDefault()` when `showSug` is true (closing only the dropdown), and `useEscape(onClose, !confirm)` on the modal only fires when the event reaches it un-prevented — exactly the "first Escape closes the dropdown, second closes the modal" behaviour the finding asked for. (Note on method: `get_by_text('Model catalog')` is ambiguous — the Settings row itself is *also* labelled "Model catalog" underneath the modal, so a naive `count()>0` check never goes to zero; the close-button `aria-label` is the reliable signal.) |
| 10. R3-S-02 (P4) | ✔ | `/v2/companies` (1440×900) → clicked the `Anthropic` row → drawer opened (`Save changes` visible) → clicked the pane at `(400,400)` — confirmed by `elementFromPoint` to be the new scrim `div[style*="position: absolute; inset: 0px; ... z-index: 29"]`, not the drawer panel — drawer closed cleanly, **no** `Discard changes?` dialog (nothing was dirty), console clean. Reopened the same row, typed into the (empty) "Alt names" field to make the draft dirty, clicked the scrim again → **`Discard changes?`** dialog appeared this time → `Cancel` → drawer stayed open, edit intact → scrim-clicked again → `Discard` → drawer closed, edit dropped (never saved — `Anthropic`'s aliases confirmed still `[]` afterward). Matches the `8804ae3` diff: the scrim `div` was added as a sibling before the drawer panel, wired to the same `onClose` the panel's own close button uses, which already routed through `drawerDirty()` → the `Discard changes?` confirm. |
| 11. R3-S-01 (P3) | ✔ | `/v2/companies` at 1024×700, rail open (default state): `document.documentElement.scrollWidth` **1024** `== innerWidth` **1024** (no page overflow); the table scroller (`.v2-scroll`) `scrollWidth` **818** `== clientWidth` **818** (no inner overflow either — matches the fix comment's own math, "1024 with the rail open = 818px of pane"); first row's `⋯` (`title="More actions"`) bounding box `x:969, width:25` → right edge **994 ≤ 1024**, visible without scrolling. Column headers at 1024: **`Company, Tier, Health, Open · 7d, Status`** — Résumés/ATS/Apps/Ø Fit all shed, confirming the `showResumes`/`showAts`/`showApps`/`showFit` thresholds (1130/998/890/842px container width) are all below the 818px pane. At 1440×900: **`Company, Tier, Health, Résumés, ATS, Open · 7d, Apps, Ø Fit, Status`** — all four previously-shed columns present. Matches the `8804ae3` diff exactly (`ResizeObserver`-driven `tblW` gating each column). |
| 12. R3-U-01 (P3) | ✔ | Measured computed colours across all three surfaces, both themes: **Applications** group-header dots (`/v2/applications` sidebar, always visible), **Stats** funnel bars (`/v2/stats`, Funnel mode), and **Sankey** node `<rect fill>` (`/v2/stats` → `Flow` toggle). **Light:** Applied `rgb(58,90,134)` · Interview `rgb(154,91,40)` · Offer `rgb(63,107,82)` · Rejected `rgb(156,59,48)` — **identical across all three surfaces**, no exceptions. **Dark:** Applied `rgb(163,190,221)` · Interview `rgb(212,160,106)` · Offer `rgb(141,187,159)` · Rejected `rgb(217,138,126)` — again identical across all three. Sankey rects also confirmed the pre-stage node (`--stage-new`, `rgb(201,195,180)` light / `rgb(74,70,56)` dark) uses its own distinct token, not one of the four application stages. Matches the `8804ae3` diff: `Applications.jsx`'s `STAGES` array, `Stats.jsx`'s funnel `rows`, and `Stats.jsx`'s `STAGE_FILL` (feeding `SankeyNode`) all now read the same `--stage-applied/-interview/-offer/-rejected` tokens from `theme.css` instead of the old mixed ramp (`--funnel-low/-mid/--accent/--line-strong` on the funnel, `--warn/--good/--bad` on Applications). |
| 13. R3-U-02 (P3) | ✔ | `/v2/stats` 30-day timeline sparkline, live `path.recharts-line-curve` computed `stroke` (2 lines per theme, "new" then "applied" in DOM order): **light** — new `rgb(184,130,43)` (`#b8822b`, `--series-new`), applied `rgb(58,90,134)` (`--stage-applied`) → **WCAG luminance ratio 2.098:1**. **dark** — new `rgb(161,115,31)` (`#a1731f`), applied `rgb(163,190,221)` → **ratio 2.199:1**. Both ≥ 2:1, matching the fix comment's own target. For context, recomputed what the *old* pairing would have given: light `--accent` `rgb(63,107,82)` vs the same `--stage-applied` → **ratio 1.151:1** — confirms the reported defect (the two lines were visually indistinguishable) and that the new token fixes it by a wide margin, not marginally. Matches the `8804ae3` diff: `Stats.jsx`'s `Spark` component now strokes "new" with `var(--series-new)` instead of `var(--accent)`; legend swatches (`[['new','var(--series-new)'], …]`) and the left Y-axis tick colour were updated to match. (Direct `getComputedStyle(document.documentElement).getPropertyValue('--series-new')` returned empty in this harness — resolved instead via the live rendered `stroke` on the chart's own `<path>`, which is the value that actually matters and needed no workaround.) |
| 14. RES-32 | ✔ | **Tailor modal** (base "PM", panel width 480 — same reproduction case round 2 used, where it measured a broken `151.75`) at **1440×900**: `getBoundingClientRect().top` = **152** (integer), `el.style.transform` = `translateY(0.5px)` (computed `matrix(1,0,0,1,0,0.5)`) — the natural unmargined top was a `.5` tie and the hook corrected it. At **1440×901**: `top` = **152** (integer), `transform` = **`""` / `none`** — natural top already landed on the grid, so no correction was applied, exactly the "or none when already integer" case the task asked to check for. **ConfirmDialog** (Companies → `⋯` → `Delete company` → dialog, then `Cancel`): `top` = **379** (integer), `transform` = `translateY(0.5px)` applied; `Cancel` closed it with no delete (`Anthropic` still present afterward, confirmed via the row locator). Matches the `8804ae3` diff exactly: `useSnapTop` now applies `transform: translateY(delta)` (a 1:1 paint-time offset) instead of the old `marginTop` fix, which — per round 2's finding — only moved a flex-centred child by *half* its value and left the tailor modal at `151.75`, not `152`. Round-2's regression, opened in round 2's own `verify.md` item 19, is fully closed. |

## Summary

**14 of 14 items ✔ — every round-3 fix verified working live.** No regressions found; no items left as "needs decision" or unverifiable in this pass (all items permitted real network / real DB writes under scratch-row rules, and the one no-LLM constraint on R3-B-04 was satisfied by testing the trim helper directly).

- **P1 (2):** R3-A-08 (undeletable scraped company/search — now orphans the audit rows instead of raising a 500), R3-A-02 (Indeed `jk` dedup collision — now protected by a per-host identity-param allowlist plus a migration that scrubs the stored setting).
- **P2 (2):** R3-A-05 (URL reader returned the ATS brand — now resolves the real employer from the board slug), R3-B-03 (résumé/cover-letter tracer link fought over one shared token — now co-owned).
- **P3 (5):** R3-B-05 (orphaned `Tailored` score after copy delete — now cleared and `best_cv` recomputed), R3-B-04 (autofill length was a hint, not a contract — now trimmed server-side on a sentence/word boundary), R3-B-02 (suggested bullets mislabelled "already applied" — now their own `suggested` chip and count), R3-S-01 (Companies row overflow at 1024px — now sheds four low-value columns by container width), R3-U-01 (funnel/Applications/Sankey stage colours disagreed — now one `--stage-*` token set everywhere).
- **P4 (4):** R3-B-06 (base résumé menu said "Delete copy" — now "Delete résumé"), R3-S-03/04 (Résumé editor head menu + Settings Model catalog didn't close on Escape — both fixed, and the catalog's two-stage Escape — dropdown first, modal second — works exactly as designed), R3-S-02 (Companies drawer had no backdrop — scrim added, wired through the existing dirty-discard confirm).
- **Re-confirmed while incidentally exercised:** RES-32 (round 2's regression — `marginTop` only shifting a flex-centred child by half its value — closed by switching to a `translateY` paint-time offset; both the previously-broken 1440×900 case and the previously-passing-by-coincidence 1440×901 case now behave correctly, and a `ConfirmDialog` instance was checked too since the task named it explicitly).

## ZZV sweep

Per-item cleanup ran as each item finished (companies/searches deleted through the UI/API under test; jobs removed by scoped SQL — no `DELETE /jobs/{id}` endpoint, same precedent as prior rounds). A final sweep (`r3v_99_sweep.py` across `/searches`, `/companies`, `/jobs?title_search=ZZV`, `/cover-letters`, plus direct SQL `ILIKE 'ZZV%'` checks on `searches`, `companies`, `jobs.title`, `jobs.company`, `resumes`, `cover_letters.json_data`, and `applications` joined to `jobs`) caught two rows the per-item scripts missed:
- Search **`ZZV Indeed`** (item 2) — its jobs were deleted by SQL but the search row itself was never explicitly deleted. Removed via `DELETE /searches/{id}` → 200.
- Company **`ZZV R3B05 Co`** (item 5) — the manual-log `POST /applications` call auto-creates a Company record as a side effect (same behavior `CLAUDE.md` documents for the Feed's "Applied" button), which the item's cleanup script didn't account for. Removed via `DELETE /companies/{id}` → 200.

Re-ran the full sweep after both deletions: **0 `ZZV` rows remaining** in `searches`, `companies`, `jobs` (by `title` and by `company`), `applications` (joined to jobs), `resumes`, and `cover_letters`. `Anthropic`'s `aliases` field (touched read/write during item 10's dirty-drawer test) confirmed back to `[]` — the edit was discarded, never saved.
