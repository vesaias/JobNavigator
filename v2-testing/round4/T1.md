# Round 4 — T1 · Backend contract + data robustness

Run: 2026-09-05, branch `v2-redesign` @ `81df231`. Everything executes inside the backend
container against the mounted source:

```bash
DOCKER="/c/Program Files/Docker/Docker/resources/bin/docker.exe"; export MSYS_NO_PATHCONV=1
"$DOCKER" compose exec -T backend sh -c "cd /app && python -m pytest backend/tests/test_r4_*.py -q -p no:cacheprovider"
```

**Final line: `725 passed, 66 xfailed, 2 warnings in 84.18s`** — no failures, no xpasses.
Whole suite with the new files included: **`1636 passed, 66 xfailed, 4 warnings in 120.67s`**.

Nothing was written to the live database. The mutating tests run on the SQLite fixture DB;
the Postgres-only checks (`test_r4_live_contract.py`) are GETs against `http://caddy`; the
backup drill ran against a scratch database (`jn_r4drill`) created and dropped inside the
db container.

Every finding below is pinned by an `xfail(strict=True, reason="R4-T1-nn")` test asserting
the **correct** behaviour, so the day it is fixed the test flips to a hard failure and has
to be un-marked. Passing tests next to each one record the *current* contract, so a fix
that changes something else is also visible.

---

## Test inventory

| File | Tests | Covers |
|---|---|---|
| `backend/tests/test_r4_dedup.py` | 40 pass / 7 xfail | 20 URL spellings, identity params, `content_hash`, malformed URLs |
| `backend/tests/test_r4_contract_jobs.py` | 78 / 10 | `/api/jobs` list, get, patch, bulk-update, save-from-extension, linkedin-import |
| `backend/tests/test_r4_contract_applications.py` | 73 / 4 | create/dupe 409, status machine, interviews, extract, listing |
| `backend/tests/test_r4_contract_searches_companies.py` | 87 / 6 | both CRUD routers, extension-search guards, delete orphaning, run 409s |
| `backend/tests/test_r4_contract_documents.py` | 112 / 10 | résumés, copy/tailor/score-check, cover letters, persona nodes, qa-bank |
| `backend/tests/test_r4_contract_settings.py` | 132 / 2 | settings ints/crons/enums/redaction, `/api/llm/models`, autofill, linkedin |
| `backend/tests/test_r4_contract_system.py` | 107 / 12 | auth, triggers + 409s, monitor, stats, telegram, `DataError` handler |
| `backend/tests/test_r4_concurrency.py` | 25 / 1 | scheduler vs manual scope collisions, stale-run recovery, monitor↔DB agreement |
| `backend/tests/test_r4_failure_injection.py` | 27 / 1 | LLM 429/529/timeout, retry+fallback, Gmail token expiry, Telegram down |
| `backend/tests/test_r4_live_contract.py` | 40 / 12 | Postgres-only: `DataError`→404, negative `LIMIT`, numeric edges (auto-skips) |
| `backend/tests/test_r4_backup.py` | 4 / 1 | dump completeness + secret content |
| `backend/tests/r4_support.py` | — | shared `client` fixture, `assert_clean`/`assert_no_leak`, row factories |

**791 tests total (725 pass, 66 xfail-strict).** Two support edits: `pytest.ini` and
`backend/tests/conftest.py` register the `live` marker (the container runs pytest from
`/app`, where the repo's `pytest.ini` is not mounted). No production code was changed.

`assert_clean(resp, *allowed)` is applied to every contract test: the status must be one of
the expected ones, must never be 500, and the body must not contain
`Traceback (most recent call last)`, `File "`, `/app/backend/`, `site-packages`,
`sqlalchemy.exc`, `psycopg2.errors` or `InterfaceError`. **No test found a stack trace or a
container path in a response body** — every 500 recorded below is a bare
`Internal Server Error` or `{"detail":"Internal server error"}`.

---

## Findings — dedup / URL normalisation

The property under test: 20 spellings of one Greenhouse posting must collapse onto one
`external_id`. **16 of 20 do; the 20 fan out to 5 distinct ids.** Three causes account for
the four that diverge (01–03 below); the twentieth spelling, "everything", combines all three.

Groups measured (`test_r4_dedup.py::test_current_spelling_fanout_is_bounded` guards the 5):

```
5f70f284  plain, scheme-upper, host-upper, path-upper, fragment, fragment-and-slash,
          utm-source, utm-bundle, fbclid, gclid, gh_src, mixed-tracking,
          apply-suffix, application-suffix, thanks-suffix, apply-suffix-upper   (16)
0d46f178  trailing-slash        → https://boards.greenhouse.io/acme/jobs/4012345/
2853f580  www-prefix
04af118b  http-scheme
a76df835  everything (http + www + trailing slash)
```

Identity params all behave: Indeed `jk`/`vjk`, Glassdoor `jl`, Dice/Monster `jobId` and
LinkedIn `currentJobId` survive normalisation (and `currentJobId` is correctly dropped on
`/jobs/view/<id>`, where the id is already in the path); the host match is strict, so
`indeed.com.evil.net` gets no identity protection. Ten Indeed postings still yield ten ids.

---

### R4-T1-01 · P2 · Trailing slash mints a second job row
`_normalize_url()` strips `/apply`, `/application`, `/thanks` and the fragment, but not a
bare trailing slash, so `…/jobs/4012345` and `…/jobs/4012345/` hash differently. Any board
that links a job with and without the slash (or a scraper that joins a base URL with a
trailing separator) creates a duplicate that no later pass can merge — `content_hash`
only catches it when company+title match exactly.
Repro: `make_external_id("Acme","PM","https://x.com/jobs/1") != make_external_id("Acme","PM","https://x.com/jobs/1/")`.
Test: `test_r4_dedup.py::test_trailing_slash_is_the_same_posting`, and the whole-property
`::test_all_twenty_spellings_share_one_external_id`.
**Status**: fixed — the folding moved into `_canonical_for_hash()` (`backend/scraper/_shared/dedup.py:154-183`), which now drops a trailing slash and then the ATS `/apply|/application|/thanks` suffix the slash had been hiding (`_fold_path`, `:134-151`). `_normalize_url()` is deliberately untouched, so `Job.url` keeps the bytes the board handed us. All 20 spellings now share one id (`test_all_twenty_spellings_share_one_external_id`, and `test_current_spelling_fanout_is_bounded` tightened 5 → 1). Live impact measured read-only in `v2-testing/round4/dup-report.md`: **1 colliding pair in 19,190 jobs**, but **6,843 rows (36%) hash to a different id than the one stored**, all but one covered by the `content_hash` fallback — a one-shot `external_id` backfill is the recommended follow-up and is written up there.

### R4-T1-02 · P3 · `www.` and the bare host are different postings
`_canonical_for_hash()` lowercases the netloc but does not fold a leading `www.`.
`https://www.example.com/jobs/1` and `https://example.com/jobs/1` are two rows.
Test: `test_r4_dedup.py::test_www_prefix_is_the_same_host`.
**Status**: fixed — `_canonical_for_hash()` strips a leading `www.` from the lowercased netloc (`backend/scraper/_shared/dedup.py:173-175`). `test_www_prefix_is_the_same_host` un-xfailed. This is the largest slice of the id shift in dup-report.md (5,934 rows).

### R4-T1-03 · P3 · `http://` and `https://` are different postings
The scheme is part of the canonical string. A board that used to link `http://` and now
links `https://` re-imports its whole catalogue once. (`Rippling`'s seeded URL in `seed.py`
is `http://ats.rippling.com/...`, so this is not hypothetical.)
Test: `test_r4_dedup.py::test_http_and_https_are_the_same_posting`.
**Status**: fixed — `http` and `https` both canonicalise to `https` before hashing (`backend/scraper/_shared/dedup.py:169-172`); other schemes are left alone so `javascript:`/junk still hash distinctly. `test_http_and_https_are_the_same_posting` un-xfailed.

### R4-T1-04 · P3 · Query params are not sorted, despite the comment saying they are
`_normalize_url()` carries the comment *"Sort params for stable hashing"* immediately above
`urlencode(cleaned, doseq=True)`, which preserves `parse_qs` insertion order. So
`?gh_jid=9&location=us` and `?location=us&gh_jid=9` hash differently. It only bites boards
whose functional params survive the tracking filter (Greenhouse `gh_jid`, Indeed `jk`
combined with anything else), but there the duplicate is silent.
Test: `test_r4_dedup.py::test_query_param_order_does_not_change_the_id`.
**Status**: fixed — the hash sorts params (`urlencode(sorted(parse_qsl(...)))`, `backend/scraper/_shared/dedup.py:177-178`). The misleading "Sort params for stable hashing" comment in `_normalize_url` is replaced by one saying why order is preserved *there* (that value is stored as `Job.url` by the company-page scraper). `test_query_param_order_does_not_change_the_id` un-xfailed; `test_normalize_url_keeps_the_trailing_slash_the_board_gave_us` is the new regression guard that the stored URL is not folded.

### R4-T1-05 · P4 · Percent-encoded unreserved characters are not normalised
`/jobs/senior%2Dpm` and `/jobs/senior-pm` are the same resource per RFC 3986 §6.2.2.2 but
hash differently.
Test: `test_r4_dedup.py::test_percent_encoded_unreserved_chars_are_equivalent`.
**Status**: deferred (v2.1)

### R4-T1-06 · P4 · `content_hash` does not collapse internal whitespace
`make_content_hash()` lowercases and strips the ends, so `"Senior  PM"` (double space, a
common artefact of HTML-derived titles) does not merge with `"Senior PM"`. Cross-source
dedup is exactly the case this hash exists for.
Test: `test_r4_dedup.py::test_content_hash_collapses_internal_whitespace`.
**Status**: deferred (v2.1)

## Findings — numeric and type edges on the HTTP surface

### R4-T1-07 · P2 · A negative `limit` is a 500 on every listing endpoint
`/api/jobs` declares `limit: int = Query(50, le=200)` with no lower bound; the other
listings take a plain `int`. Postgres rejects `LIMIT -5`
(`InvalidRowCountInLimitClause` → SQLAlchemy `DataError`), the `DataError` handler sees a
message that is not a uuid error, and answers `{"detail":"Internal server error"}`.
Confirmed live on **seven** endpoints:
`/api/jobs?limit=-5`, `/api/jobs/unscored-ids?limit=-5`, `/api/monitor/history?limit=-5`,
`/api/monitor/finished?limit=-5`, `/api/activity-log?limit=-3`, `/api/scrape-log?limit=-3`,
`/api/applications?limit=-3`. (`offset` is already clamped with `max(0, offset)`.)
Correct behaviour: 422, the same as `limit=abc` gets today.
Tests: `test_r4_live_contract.py::test_negative_limit_is_rejected_not_a_500` (7 params),
plus SQLite-side pins in `test_r4_contract_jobs.py`, `..._applications.py`, `..._system.py`.
**Status**: fixed — all seven listings now declare a lower bound, so a negative `limit` is a 422 from the validation layer instead of `LIMIT -5` reaching Postgres: `/api/jobs` and `/api/jobs/unscored-ids` (`backend/api/routes_jobs.py:102-103`, `:251`), `/api/applications` (`backend/api/routes_applications.py:314-315`), `/api/monitor/history`, `/api/monitor/finished`, `/api/activity-log`, `/api/scrape-log` (`backend/main.py:1000`, `:943-944`, `:1058-1059`, `:1090-1091`). The floor is `ge=0`, not `ge=1` — an empty page stays a legal request. `offset` keeps its existing `max(0, offset)` clamp on the monitor/activity routes (pinned by `test_negative_offset_stays_clamped_not_rejected`), matching the round-2 precedent for clamping. All params moved to the `Annotated[int, Query(...)]` form so the handlers stay callable directly from tests. Regressions: `test_negative_limit_is_422_on_every_job_listing`, `test_negative_limit_is_422_on_every_system_listing`, `test_list_applications_negative_limit_is_422`, `test_zero_limit_stays_legal`.

### R4-T1-08 · P3 · `/api/stats/timeline?days=999999999` escapes as an untyped 500
`datetime.now() - timedelta(days=days)` overflows; nothing catches it, so the response is
the bare Starlette body `Internal Server Error` (not even the JSON shape the rest of the
API uses). `days=-5` is harmless (empty result).
Tests: `test_r4_live_contract.py::test_timeline_rejects_an_out_of_range_day_count`,
`test_r4_contract_system.py::test_timeline_rejects_an_out_of_range_day_count`.
**Status**: fixed — `days` is bounded on the top only (`Annotated[int, Query(le=3650)]`, `backend/main.py:1128`), so `999999999` is a 422 while `days=0` and `days=-5` keep their existing "empty window" meaning. `/api/stats/llm-costs` was deliberately left alone: `days<=0` there means *all time* and is a shipped, verified contract behind the Stats period pills (`v2-testing/stage3/persona-stats.md:308`). Regressions: `test_timeline_still_accepts_the_windows_the_ui_uses` (5 params), `test_timeline_rejects_a_window_that_overflows_timedelta` (3 params).

### R4-T1-09 · P3 · A malformed uuid in a *filter* answers 404 "Not found"
`GET /api/jobs?search_id=abc` and `GET /api/cover-letters?job_id=abc` return
`404 {"detail":"Not found"}` — the `DataError` handler cannot tell a bad path segment from
a bad query value. A list endpoint reporting "Not found" for a bad filter is misleading, and
it is inconsistent with `/api/monitor/finished?job_ids=abc` and
`/api/monitor/in-flight?job_ids=abc`, which drop unparseable ids and return `200 []`/`{}`.
Correct behaviour: 422 for a filter, 404 only for a path segment.
Test: `test_r4_live_contract.py::test_malformed_uuid_in_a_filter_should_be_422`; the current
behaviour is recorded in `::test_malformed_uuid_in_a_filter_is_currently_404` and the
graceful path in `::test_monitor_filters_drop_unparseable_ids_instead`.
**Status**: fixed — a malformed uuid in a *filter* is now a 422 from `uuid_filter()` (`backend/api/_input.py:35-50`), applied to `/api/jobs?search_id` (`routes_jobs.py:106`, plus the shared `_apply_common_filters` at `:232-233`) and `/api/cover-letters?job_id` (`routes_cover_letters.py:150-155`). **This does not revert F-007** (`v2-testing/FINDINGS.md:58-63`, the global `DataError` → 404 handler): a malformed uuid in a *path segment* still answers 404, which the ten `test_malformed_uuid_in_a_path_is_404` params keep pinning. Only the filter case is carved out. The companion `test_malformed_uuid_in_a_filter_is_currently_404` was renamed to `..._is_not_a_404` and now records the new contract. Regressions: `test_malformed_search_id_filter_is_422_not_404`, `test_a_valid_search_id_filter_still_filters`, `test_cover_letters_malformed_job_id_filter_is_422_not_404`, `test_cover_letters_valid_job_id_filter_still_works`.

### R4-T1-10 · P3 · `/api/health/entities?window` accepts 0 and negatives
`window=0` makes `len(recent) < 0` false and `all([])` true, so **every active company and
search is reported as broken** with the reason *"No results in the last 0 scrapes"* —
verified live against the real 126 companies. `window=-1` is a 500 (negative `LIMIT`, same
root cause as R4-T1-07).
Tests: `test_r4_live_contract.py::test_health_entities_rejects_a_negative_window`,
`::test_health_entities_window_zero_does_not_flag_everything`,
`test_r4_contract_system.py::test_health_entities_rejects_a_non_positive_window`.
**Status**: fixed — `window` is `Annotated[int, Query(ge=1, le=100)]` (`backend/main.py:1164`), so `0` and negatives are 422 and the `all([])` trap is unreachable. The endpoint's deliberate "three bad runs" default and its inactive-entity exclusion are untouched. `Annotated` rather than a `Query(...)` default matters here: `test_health_entities.py` calls `get_failing_entities()` directly, and a `Query` object as the default value would reach `.limit()`. Regressions: `test_health_entities_rejects_a_non_positive_window_value` (3 params), `test_health_entities_still_answers_for_a_sane_window`.

### R4-T1-11 · P3 · `PATCH /api/jobs/{id}` writes caller values into typed columns unchecked
The handler copies any key in `{seen, saved, status}` straight onto the model, so
`{"saved": "banana"}` reaches the driver and 500s. `status` is a free `String` column, so
`{"status": "banana"}` is accepted verbatim and silently creates a status the feed cannot
render (recorded as current contract, not a defect, in
`::test_patch_job_unknown_status_value_is_accepted_verbatim`).
Test: `test_r4_contract_jobs.py::test_patch_job_rejects_a_wrongly_typed_value`.
**Status**: fixed — `_validate_job_updates()` (`backend/api/routes_jobs.py:515-535`) type-checks the three allowed columns before anything is written, and is called from both `PATCH /api/jobs/{id}` (`:597`) and `POST /api/jobs/bulk-update` (`:550`). `seen`/`saved` must be real booleans (an int `1` is rejected too, since `bool` subclasses `int` and the guard tests `isinstance(v, bool)` explicitly); `status` must be a string. Keys outside the allow-list are still dropped silently, and `status` stays a free string by design — `test_patch_job_unknown_status_value_is_accepted_verbatim` and the emoji round-trip both still pass. Regressions: `test_patch_job_rejects_every_wrongly_typed_column` (7 params), `test_patch_job_still_accepts_correctly_typed_values` (4), `test_bulk_update_rejects_a_wrongly_typed_column`.

### R4-T1-12 · P2 · One malformed id aborts the whole bulk update
`POST /api/jobs/bulk-update` documents `{"updated": n, "not_found": [...]}` so the frontend
can reconcile stale selections, but it looks each id up individually — a non-uuid raises
`DataError` mid-loop, so the request answers `404 {"detail":"Not found"}`, the single
`db.commit()` after the loop is never reached, and **every valid id in the batch is
silently dropped too**. The client is told "Not found" without being told which id, so it
cannot self-correct. A stale client-side selection is exactly what produces this.
Test: `test_r4_contract_jobs.py::test_bulk_update_reports_a_malformed_id_instead_of_aborting`.
**Status**: fixed — the loop parses each id itself and routes an unparseable one into `not_found` instead of letting `DataError` abort the batch (`backend/api/routes_jobs.py:541-568`). **The response shape is unchanged**: still `{"updated": <int>, "not_found": [<ids>]}`, because `test_bulk_update_reports_missing_ids` and `test_bulk_update_empty_id_list_is_a_noop` assert exact dict equality and the FEED-20 frontend toast reads those two keys (`v2-testing/stage3/feed.md:259`). The suggested `{updated: [...], skipped: [...]}` shape was therefore not adopted — malformed ids join the existing `not_found` list, which is what the client already knows how to reconcile. A non-list `job_ids` or non-dict `updates` is a 422. Regressions: `test_bulk_update_keeps_going_past_several_malformed_ids` (asserts both valid rows were actually written), `test_bulk_update_rejects_wrongly_shaped_payloads`.

## Findings — unhandled input shapes (500s)

None of these leak a trace — they are all bare `Internal Server Error` — but each is an
unguarded crash on input the Chrome extension or a stale frontend can send.

### R4-T1-13 · P3 · `POST /api/jobs/linkedin-import` 500s on a non-JSON body
The handler opens with a bare `await request.json()`. A truncated or non-JSON body from the
extension is an unhandled `JSONDecodeError`.
Test: `test_r4_contract_jobs.py::test_linkedin_import_non_json_body_is_422_not_500`.
**Status**: fixed — `await request.json()` is wrapped, and a non-object body is rejected up front (`backend/api/routes_jobs.py:27-34`). Regression: `test_linkedin_import_junk_body_is_422` (4 payload shapes, including empty and truncated JSON).

### R4-T1-14 · P3 · `linkedin_ids` is iterated without a type check
`{"linkedin_ids": 5}` → `TypeError` → 500. `{"linkedin_ids": "4012345"}` is worse: the
string is iterated per character, so the endpoint answers `200 {"accepted": 7}` and queues
seven junk ids for the Voyager scraper.
Tests: `test_r4_contract_jobs.py::test_linkedin_import_non_iterable_ids_is_422_not_500`,
`::test_linkedin_import_string_ids_is_rejected_not_split_into_characters`.
**Status**: fixed — `linkedin_ids` must be a list/tuple; a bare string, an int, a float, a dict or a bool is a 422 rather than being iterated (`backend/api/routes_jobs.py:35-43`). `None` and `[]` still take the documented `200 {"accepted": 0}` path that round 2 recorded (`v2-testing/round2/happy-path.md:174`) — pinned by `test_linkedin_import_empty_ids_stays_a_200_noop`. Regression: `test_linkedin_import_rejects_non_list_ids` (5 params).

### R4-T1-16 · P4 · A list/dict `status` 500s the application PATCH
`updates["status"] not in VALID_STATUSES` does a set membership test on an unhashable
value → `TypeError` before the 400 guard can fire. Strings, numbers and `None` are all
rejected correctly with 400.
Test: `test_r4_contract_applications.py::test_patch_application_rejects_an_unhashable_status`.
**Status**: deferred (v2.1)

### R4-T1-19 · P3 · A non-numeric `tier` 500s `POST /api/companies/bulk-activate`
`tier_ints = [int(t) for t in data.tiers if t != 'none']` on `BulkActivate.tiers: List[str]`
— any free-text tier is an unguarded `ValueError`. The `"none"` path itself works
(`::test_bulk_activate_none_tier_selects_untiered_companies`).
Test: `test_r4_contract_searches_companies.py::test_bulk_activate_rejects_a_non_numeric_tier`.
**Status**: fixed — `int(t)` is guarded and a free-text tier is a 422 (`backend/api/routes_companies.py:327-337`). The `'none'` sentinel and the mixed `['none', '1']` form still work. Regressions: `test_bulk_activate_rejects_every_non_numeric_tier` (5 params), `test_bulk_activate_still_accepts_real_tiers` (4 params).

### R4-T1-20 · P2 · `.strip()` on a caller-supplied non-string 500s four endpoints
One idiom, four handlers: `(body.get(k) or "").strip()` / `body.get(k, "").strip()` raises
`AttributeError` whenever the value is a number, list or dict.
- `POST /api/resumes` (`name`)
- `POST /api/cover-letters` (`name`)
- `POST /api/persona/qa-bank` (`question`, `answer`)
- `POST /api/jobs/save-from-extension` (`title`, `company`, `url`)
All four already answer 400 for `""`/`None`, so the fix is a type check, not a new branch.
Tests: `test_r4_contract_documents.py::test_create_resume_wrongly_typed_name_is_400_not_500`,
`::test_create_cover_letter_wrongly_typed_name_is_400_not_500`,
`::test_qa_bank_wrongly_typed_fields_are_400_not_500`,
`test_r4_contract_jobs.py::test_save_from_extension_wrongly_typed_title_is_400_not_500`.
**Status**: fixed — one shared helper, `str_field()` (`backend/api/_input.py:15-33`), replaces the `(body.get(k) or "").strip()` idiom on all four handlers: `POST /api/resumes` (`routes_resumes.py:594-596`), `POST /api/cover-letters` (`routes_cover_letters.py:161-162`), `POST /api/persona/qa-bank` (`routes_persona.py:284-289`) and `POST /api/jobs/save-from-extension` (`routes_jobs.py:373-378`, all four of `title`/`company`/`url`/`description`). A non-string value now gets the same **400** these endpoints already answer for a blank one, so the frontend's existing error handling needs no change. Regressions: `test_create_resume_rejects_every_wrongly_typed_name` (7 params), `test_create_cover_letter_rejects_every_wrongly_typed_name` (7), `test_qa_bank_rejects_every_wrongly_typed_field` (8), `test_qa_bank_still_appends_a_well_typed_pair`, `test_save_from_extension_rejects_every_wrongly_typed_string` (16).

### R4-T1-23 · P3 · `POST /api/analyze/{job_id}` 500s on a malformed job id
`target_job_id=_uuid.UUID(job_id)` is evaluated in the `launch_background(...)` call, before
any lookup, so a bad id raises `ValueError` rather than answering 404 the way every other
id route does.
Test: `test_r4_contract_system.py::test_analyze_trigger_with_a_malformed_job_id_is_404_or_422`.
**Status**: fixed — the job id is parsed before anything else and a malformed one answers **404**, matching every other id route and the F-007 handler it was bypassing (`backend/main.py:446-451`, with the parsed value reused at `:465`). Regression: `test_analyze_trigger_malformed_job_id_is_404` (3 params).

### R4-T1-24 · P3 · A non-string `api_key` 500s `/api/auth/verify` and `/api/auth/set-session`
`hmac.compare_digest(5, expected)` raises `TypeError`. `""`, `None`, `[]` and `{}` are
falsy and correctly answer 401; only truthy non-strings crash. It is an unauthenticated
endpoint, so anyone who can reach the port can produce the 500.
Tests: `test_r4_contract_system.py::test_verify_wrongly_typed_key_is_401_not_500`,
`::test_set_session_wrongly_typed_key_is_401_not_500`.
**Status**: fixed — a non-string `api_key` is coerced to `""` before it reaches `hmac.compare_digest`, on both `/api/auth/verify` and `/api/auth/set-session` (`backend/main.py:172-177`, `:194-199`), so a truthy non-string is simply "not the key" and answers 401 like every other wrong value. Regressions: `test_auth_wrongly_typed_key_is_401` (2 endpoints × 5 shapes) and `test_auth_still_accepts_the_real_key`.

### R4-T1-25 · P4 · An out-of-range `since` 500s `/api/monitor/finished`
The handler already intends to swallow a bad `since`
(`except (ValueError, TypeError, OverflowError): since_dt = None`) but
`datetime.fromtimestamp(1e17, tz=utc)` raises outside that set, so
`?since=99999999999999999999` is a 500. `since=abc`, `-1` and `0` are handled.
Test: `test_r4_contract_system.py::test_monitor_finished_out_of_range_since_is_ignored`.
**Status**: deferred (v2.1)

### R4-T1-26 · P4 · A null `message` 500s the Telegram webhook
`callback_query.get("message", {}).get("message_id")` — `.get`'s default only fires when
the key is absent, not when it is explicitly `null`. Telegram itself will not send that
shape, but the endpoint is reachable by anyone holding the webhook secret, and the other
malformed shapes (`callback_query: null`, `{}`, `{"data": null}`) are all handled.
Test: `test_r4_contract_system.py::test_telegram_webhook_null_message_is_not_a_500`.
**Status**: deferred (v2.1)

## Findings — missing validation (no crash, wrong data)

### R4-T1-15 · P2 · An application can leave `rejected` with a plain PATCH
There is no ordering or terminality guard: `rejected → interview` and `offer → applied` are
accepted and written into `status_transitions`, which is what the Stats funnel and the
Sankey are built from. One misclick in the Applications board puts a backwards edge in the
chart permanently. Skipping a stage (`applied → offer`) is deliberate and fine; reviving a
terminal state probably should be an explicit reopen.
Test: `test_r4_contract_applications.py::test_patch_application_cannot_leave_a_terminal_rejected_state_silently`.
Current behaviour recorded in `::test_patch_application_backwards_transition_is_allowed_and_logged`
and `::test_patch_application_out_of_order_skips_a_stage`.
**Status**: needs decision (conflicts with `v2-testing/stage3/applications.md:194`) — that line records `applied→interview`, `interview→offer`, `offer→rejected`, **`rejected→applied`** under the heading *"Verified working (measured, no defect)"* (`:192`), i.e. leaving a terminal state was explicitly measured and signed off in stage 3. The Applications board's stage stepper is a plain four-way picker (`frontend/src/v2/Applications.jsx:445`, `onStage={(s) => patch(d.id, { status: s })}`) with **no undo toast and no `force` affordance** for a status change — the only undo on that screen is for interviews (`:231`). A 409 would therefore strand any application a user moves to `rejected` by mistake, and the "allow the exact reverse of the last transition" escape hatch does not cover the very sequence stage 3 signed off: there the previous edge is `offer→rejected`, so `rejected→applied` is not its reverse. Implementing this needs a product decision plus a UI affordance (an explicit "Reopen" action, or a `force` flag the stepper sends), not just a backend guard. Left unimplemented; `test_patch_application_cannot_leave_a_terminal_rejected_state_silently` keeps its xfail-strict marker.

### R4-T1-17 · P3 · Intervals accept negative and zero values
`Search.run_interval_minutes = -30` and `Company.scrape_interval_minutes = 0` are stored
verbatim. `/api/scheduler/jobs` then renders `"Every -30 min (search override)"` and
`"Every 0 min (company override)"`; the orchestrator's own guard is what actually decides
whether the entity runs, so the schedule panel disagrees with reality. Contrast
`PATCH /api/settings`, which does reject negatives for the same class of value.
Tests: `test_r4_contract_searches_companies.py::test_create_search_rejects_a_negative_interval`,
`::test_create_company_rejects_a_zero_scrape_interval`. The stored-verbatim behaviour is
recorded in `::test_create_search_stores_out_of_range_numbers_verbatim` (8 params) and
`::test_create_company_stores_a_non_positive_interval_verbatim`.
**Status**: deferred (v2.1)

### R4-T1-18 · P3 · `search_mode` has no enum guard on create
`POST /api/searches {"search_mode": "banana"}` is accepted; the orchestrator falls through
and the search silently never runs. Nothing stops `"extension"` or `"linkedin_extension"`
either, which would produce extra rows the UI treats as the two built-in searches —
undeletable and unrunnable by design (that guard is verified in
`::test_extension_searches_refuse_delete_run_and_acknowledge`). `PATCH /api/settings`
validates the analogous enums; this router does not.
Test: `test_r4_contract_searches_companies.py::test_create_search_rejects_an_unknown_search_mode`.
**Status**: deferred (v2.1)

### R4-T1-21 · P4 · Integer settings have a floor but no ceiling
`scrape_interval_minutes = 10**18` passes validation and reaches
`IntervalTrigger(minutes=10**18)`. Not a crash today, but the guard exists precisely so a
bad value cannot reach `configure_scheduler()` inside the lifespan.
Test: `test_r4_contract_settings.py::test_patch_settings_rejects_an_absurdly_large_interval`.
**Status**: deferred (v2.1)

### R4-T1-22 · P2 · The streaming autofill ignores the editable `autofill_prompt` setting
`POST /api/autofill/answer` reads `autofill_prompt` from settings and 500s with a named
reason when it is empty. `POST /api/autofill/answer/stream` — the variant the extension
actually uses to render into the field — builds its own hardcoded prefix and never looks at
the setting. Editing the prompt in Settings therefore has no effect on the answers the user
sees. (The divergence is documented in the code as deliberate, to stop a JSON wrapper
leaking into the streamed text; the finding is that the setting is still presented as
governing autofill.)
Test: `test_r4_contract_settings.py::test_autofill_stream_uses_the_editable_prompt_setting`.
**Status**: fixed — both variants now build their prompt from the editable `autofill_prompt` setting through one shared `_build_autofill_prompt()` (`backend/api/routes_autofill.py:210-269`); the streaming handler's hardcoded prefix is gone (`:304-332`). The deliberate part of the old divergence is preserved rather than reverted: the JSON-envelope suppression that stopped a `{"answer": ...}` wrapper leaking into the streamed field is now a suffix appended to the template's output, not a fork of the whole prompt, and the refinements block still rides on the suffix so the cache prefix stays stable. Server-side length trimming on `/answer` (a shipped decision, `v2-testing/REPORT-final.md:46`) is untouched. Regressions: `test_autofill_stream_and_answer_read_the_same_prompt_setting` (asserts a sentinel from the setting reaches the streamed cache prefix, and that question/company reach the suffix) and `test_autofill_stream_still_suppresses_the_json_envelope`.

## Findings — concurrency and run bookkeeping

Everything asked for here holds. `tracked_run` (scheduler) and `launch_background` (manual)
share one `_running` dict, and the second caller on a scope key is refused in both
directions: manual-then-scheduler raises `JobAlreadyRunningError`, which each of the seven
scheduled jobs catches and logs rather than propagating to APScheduler; scheduler-then-manual
answers **409** on all nine trigger endpoints plus `/api/searches/{id}/run`,
`/api/scrape/company/{id}`, `/api/resumes/tailor`, `/api/cover-letters/generate`,
`/api/analyze/{job_id}` and `/api/jobs/linkedin-import`. Different scope keys of one
job type run side by side (two company scrapes, two `analyze_job` runs with different
`cv_ids`). Ten runs across two scopes leave zero in-memory entries and zero `running` rows.
`cleanup_stale_runs()` marks every orphan `running` row `failed` with
`error="Process restarted"`, a `finished_at` and a positive `duration_seconds`; it is
idempotent, safe on an empty table, and the recovered run shows as failed in
`/api/monitor/history`. `/api/monitor/active`, `/api/monitor/in-flight` and
`/api/monitor/finished` all agree with `_running` and with the `JobRun` rows, before and
after completion, and a failure reason stays server-side.

One finding:

### R4-T1-27 · P4 · `cleanup_stale_runs()` does not consult the in-memory monitor
It fails every `running` row unconditionally. Today that is safe because the only caller is
the lifespan, where `_running` is empty by construction. It is a latent trap for any future
caller (a health endpoint, a watchdog, a manual "clear stuck runs" action): a live run would
be marked failed in the DB while `/api/monitor/active` still shows it running.
Test: `test_r4_concurrency.py::test_stale_recovery_should_skip_runs_the_monitor_still_owns`;
the current behaviour and its rationale are recorded in
`::test_stale_recovery_does_not_touch_a_run_that_is_genuinely_live`.
**Status**: deferred (v2.1)

## Findings — failure injection

Injected `429`, `529`, read timeout and connection reset at `llm_client._dispatch`, an
`invalid_grant` from the Gmail token endpoint, and a dead Telegram (network error, HTTP 500,
timeout). Results:

- **LLM down → the job is left unscored.** `cv_scores` stays empty, `best_cv_score` stays
  `None`, and `score_single_job` returns `"Scoring failed"` for all four fault kinds. A
  malformed `{"scores": null}` response is likewise not persisted. One `llm_call_log` row is
  written per attempt-set with `success=False` and a non-empty `error`.
- **Retry + fallback are exactly as documented.** The primary is tried 4 times, then the
  fallback once; a transient first failure never reaches the fallback; with both providers
  down a single `RuntimeError` names both pairs; with no fallback configured the primary
  error propagates. The `llm_call_log` row names the pair that **answered** (`openai` /
  `fallback-model`), not the one that timed out.
- **Gmail token expiry is not a crash.** `_get_access_token()` returns `""`, `check_emails()`
  returns immediately, and the scheduled run finishes `completed` with
  `"No new replies"`. A genuine network error does mark the run `failed` with the reason on
  the `JobRun` row, frees the scope, and lets APScheduler handle the exception. A 403 on the
  messages list is swallowed for the next tick.
- **Telegram down does not fail anything.** `_send_message()` returns `False` for a network
  error, a timeout and an HTTP 500, and `False` when unconfigured. `check_scrape_health()`
  raises nothing even when the send raises, and `run_all_scrapes()` still finishes
  `completed` with its scrape summary intact. `POST /api/telegram/test` still answers 202 and
  records the failure only in the run history.

One finding:

### R4-T1-28 · P2 · An LLM outage produces a `completed` run
`_score_job_inner` catches the exception and returns `None`; `score_single_job` turns that
into the string `"Scoring failed"`; `launch_background` treats any returned string as a
successful summary. So the `JobRun` row reads `status="completed"`,
`result_summary="Scoring failed"`, `error=None`. `/api/monitor/finished` — which the v2
dashboard uses to decide OK/NOK for an op that just left `/monitor/in-flight` — therefore
reports a provider outage as a success toast, and Stats › Run history shows it green. The
only place the failure is visible as a failure is the `llm_call_log` table.
Test: `test_r4_failure_injection.py::test_llm_failure_marks_the_run_failed_not_completed`;
the current behaviour is recorded in `::test_llm_failure_run_is_visible_in_the_run_history`.
**Status**: fixed — a tracked coroutine can now report a handled failure without giving up its summary. `job_monitor.mark_run_failed(reason)` sets a per-task `ContextVar` (`backend/job_monitor.py:44-72`) that both `launch_background`'s wrapper (`:255-262`) and `tracked_run` (`:211-218`) read when recording the run, so the `JobRun` lands as `status="failed"` with the reason on `error` while `result_summary` keeps the user-facing string. The scorer sets it whenever an LLM call fails, with the provider error attached (`backend/analyzer/cv_scorer.py:349-359`). `score_single_job` still returns `"Scoring failed"`, so the existing contract tests are untouched, and `/api/monitor/finished` now reports a provider outage as a failure instead of a success toast. Regressions: `test_every_llm_fault_kind_marks_the_run_failed` (4 fault kinds, asserts status, summary *and* error), `test_a_healthy_run_is_still_completed`, `test_a_failure_flag_does_not_leak_into_the_next_run`.

## Findings — backup / restore drill

Ran against a scratch database inside the db container; the live database was never
touched. Dump under test: `backups/round4_baseline_20260905.dump` (`pg_dump -Fc`,
23,853,242 bytes, taken 2026-09-05 11:55).

```
createdb -U $POSTGRES_USER jn_r4drill
pg_restore -U $POSTGRES_USER -d jn_r4drill --no-owner --no-privileges  < dump   # rc=0, no errors
… count comparison …
dropdb -U $POSTGRES_USER jn_r4drill                                             # 0 remaining
```

| table | live | restored | delta |
|---|---:|---:|---:|
| activity_log_v2 | 8386 | 8385 | +1 |
| applications | 377 | 377 | — |
| companies | 126 | 126 | — |
| cover_letters | 19 | 18 | +1 |
| interviews | 0 | 0 | — |
| job_runs | 1636 | 1635 | +1 |
| jobs | 19190 | 19189 | +1 |
| llm_call_log | 4457 | 4457 | — |
| personas | 1 | 1 | — |
| resumes | 353 | 352 | +1 |
| scrape_log | 6846 | 6845 | +1 |
| searches | 6 | 6 | — |
| settings | 87 | 87 | — |
| tracer_click_events | 0 | 0 | — |
| tracer_links | 628 | 628 | — |
| visa_cache | 1441 | 1440 | +1 |

16 tables, 9 identical, 7 off by exactly +1. Verified that all seven deltas are rows created
**after** the dump timestamp (one saved job and the documents it produced), not rows lost in
transit:

```sql
select count(*) from jobs          where discovered_at > '2026-09-05 11:55+03';  -- 1
select count(*) from resumes       where created_at    > '2026-09-05 11:55+03';  -- 1
select count(*) from cover_letters where created_at    > '2026-09-05 11:55+03';  -- 1
select count(*) from job_runs      where started_at    > '2026-09-05 11:55+03';  -- 1
select count(*) from scrape_log    where ran_at        > '2026-09-05 11:55+03';  -- 1
select count(*) from activity_log_v2 where created_at  > '2026-09-05 11:55+03';  -- 1
select count(*) from visa_cache    where fetched_at    > '2026-09-05 11:55+03';  -- 1
```

**The restore is lossless.** No missing table, no missing row, no pg_restore warning.

Secret scan: the raw custom-format dump is compressed, so the check runs against the
plain-SQL rendering (`pg_restore -f -`, 86,997,812 bytes) as well as the raw bytes.

| `.env` key | in dump |
|---|---|
| ANTHROPIC_API_KEY | **found** (restored SQL) |
| INITIAL_API_KEY | **found** (restored SQL) |
| CLAUDE_CODE_OAUTH_TOKEN | not present |
| TELEGRAM_BOT_TOKEN | not present |
| GMAIL_CLIENT_ID / GMAIL_CLIENT_SECRET / GMAIL_REFRESH_TOKEN | not present |
| DATABASE_URL | not present |

### R4-T1-29 · P2 · Database dumps carry the LLM key and the dashboard key in clear
Both values live in the `settings` table (`llm_api_key`, `dashboard_api_key`) because the
app is configured from the dashboard by design, so `pg_dump` writes them out verbatim. The
scheduled `db_backup` job drops these files into `/app/backups` — which is
`V:\JTrakProject\backups` on the host, inside the repository — every night, keeping five.
`GET /api/settings` redacts both correctly (verified live: no `.env` value appears in the
response), and no route serves a dump file, so this is an at-rest concern, not an exposure
through the API. Options: redact secret-shaped settings from scheduled dumps, move
`backups/` out of the repo tree, or accept and document it.
Tests: `test_r4_backup.py::test_the_dump_contains_no_env_secret_in_clear`, with
`::test_which_env_secrets_reach_the_dump_is_recorded` pinning exactly which two keys leak so
a partial fix is visible; `::test_the_dump_carries_the_whole_settings_table` guards against
a dump that silently loses rows.
**Status**: fixed (documented) — no code change, as scoped. `README.md` gains a **Backups** section above Security saying that the scheduled `db_backup` dump contains the `settings` table verbatim, therefore `llm_api_key` and `dashboard_api_key` in clear; that dumps are to be treated exactly like `.env` (local-only, never committed, never shared); and that `backups/` is in `.gitignore` for that reason, with a note to re-check that if the backup path moves. The two xfail-strict tests in `test_r4_backup.py` are **left in place on purpose**: the accepted option was "document it", so the dump content is deliberately unchanged and `test_the_dump_contains_no_env_secret_in_clear` still pins the real state. If a future pass chooses redaction instead, that test flips and has to be un-marked.

---

## Contract behaviour confirmed correct (no finding)

Recorded as passing tests so a future change to any of it shows up as a failure:

- **Auth.** Every `/api/*` route is 401 without a key and 401 with a wrong key; the
  `jn_session` cookie is accepted in place of the header; `/health` and `/cv/{token}` are
  public; a bare `/` still needs a key (the `?cv` bypass is narrow); `GET /api/settings`
  redacts `dashboard_api_key`, `*_api_key`, `*_password`, `*_secret`, `*_session_id` and
  `gmail_refresh_token`, returns `""` (not six bullets) for an unset secret, and the
  redaction placeholder round-trips through `PATCH` without overwriting the stored value.
- **The `DataError` → 404 handler.** A uuid syntax error maps to `404 {"detail":"Not found"}`;
  every other `DataError` maps to `500 {"detail":"Internal server error"}` with the driver
  message stripped. Verified live on ten id routes.
- **Duplicate submits.** A second application for the same posting is `409` carrying the
  existing `application_id`, leaves no half-written job row, and is caught even when the
  second URL only differs by tracking params. A duplicate company name is `409`
  case-insensitively.
- **Settings validation.** Integers reject `"abc"`, `""`, `"1.5"`, `"1e3"`, `"0x10"`,
  `None`, `[]`, `{}`, booleans and negatives; the five `*_cron` keys reject 4-field,
  6-field, `@daily`, `* * * * mars` and non-string values while accepting `""` (disabled);
  all seven provider enums and the four depth/style enums reject unknown values
  case-insensitively; unknown keys reject the **whole batch**, leaving valid keys in it
  unwritten. A failing `configure_scheduler()` is reported as a `warnings` entry on a 200,
  not a 500.
- **Allow-lists hold.** `PATCH` on jobs, applications, searches, companies, résumés and
  cover letters silently drops every key outside its allow-list — `id`, `created_at`,
  `status_transitions`, `last_run_at`, `last_scraped_at` and `company` were all tried and
  none was written. (Settings and Persona are the two that reject unknown keys with 400
  instead; the inconsistency is deliberate per-router and is recorded, not filed.)
- **Deletes orphan rather than cascade where they should.** Deleting a search nulls
  `Job.search_id` and `ScrapeLog.search_id` and keeps the rows; deleting a company nulls
  `ScrapeLog.company_id`; deleting a base résumé cascades to its tailored children;
  deleting an application releases the job back to `saved`. Second delete is 404 in every case.
- **Extension searches are protected.** `DELETE`, `/run` and `/acknowledge` all answer 409
  for both `extension` and `linkedin_extension` modes.
- **Unicode and size.** Emoji + CJK round-trip through job titles, company names, résumé and
  cover-letter names, persona nodes, settings prompts and qa-bank answers. 10 MB bodies were
  sent to nine endpoints (job description, application notes, search term, company notes,
  résumé `json_data`, cover-letter body, persona node, qa-bank answer, scoring rubric) and
  every one answered 200/201 without a 500. A 6 kB query string is fine. A 10 000-id
  bulk-update is fine. An 11 MB PDF import is rejected by the size guard.
- **Wrongly typed query params are already 422** (`min_score=abc`, `remote=maybe`,
  `limit=abc`, `days=abc`, `tier=abc`, `active=maybe`), and unknown filter *values*
  (`status=zzz`, `source=zzz`) correctly return an empty page rather than an error.
- **Long-running triggers.** `/api/llm/models` answers 400 for an unsupported provider, 400
  when no key is configured, 502 (never 500) when the provider rejects the key or the
  network fails, and serves a stale cache with `"stale": true` when it has one.

## Notes for the fix loop

- R4-T1-07, R4-T1-08 and R4-T1-10 are the same shape (an unvalidated integer reaching SQL or
  `timedelta`) and are probably one change: a `Query(..., ge=1, le=...)` sweep across the
  listing and window params.
- R4-T1-13/14/16/19/20/23/24/25/26 are all "unguarded shape reaching a builtin". A shared
  helper (`_as_str(body, key)` / `_as_list(body, key)`) plus Pydantic models on the three
  `body: dict` endpoints would close most of them.
- R4-T1-01..05 are all in `_normalize_url` / `_canonical_for_hash`. **Changing any of them
  changes every existing `external_id`**, so a fix needs a backfill plan, not just a code
  edit — `test_r4_dedup.py::test_current_spelling_fanout_is_bounded` will show the
  improvement (5 → 1) the moment it lands.
- The suite is fast (85 s for all 791) because `r4_support._no_outbound` neutralises the
  fire-and-forget page fetch and H-1B lookup that `BackgroundTasks` runs inline under
  TestClient; without it every application create costs ~6 s of real network.
