# T3 — Visual review across themes

Scope per `ROUND4-PLAN.md`: 13 routes × 5 themes (Default, Board, Cobalt, SaaS, Win98) ×
light/dark × 1440/1024, reviewed at 1440 (130 screenshots); Firefox + Chromium spot
check for Default; PDFs (résumé × 8 templates, one tailored copy, one cover letter)
page-by-page; a WCAG contrast table per theme; the Cobalt/SaaS sameness list and the
Win98 depth-delta list as design-brief inputs. Screenshots captured with
`v2-testing/tools/shots.py <stage> --theme <name>` against the paused live stack
(scheduler off for the capture window, restored after) and live under
`v2-testing/artifacts/round4/t3/<theme>/`. This pass does not repeat verdicts already
recorded in `v2-testing/round-design/proof-skins-A.md` (static token/contrast audit)
and `proof-skins-B.md` (visual review of Cobalt/SaaS/Win98 against the default
baseline) — only what is new, changed since, or was missed there (including a
dedicated fresh pass on the Default and Board themes, neither of which had a
standalone review before this round).

WebKit could not be tested: the backend container's Debian trixie base has no
compatible package for the exact old sonames (`libicudata.so.66`, `libjpeg.so.8`, …)
Playwright's WebKit 2092 build (an ubuntu20.04 fallback, since this OS isn't
officially supported) requires; Firefox's dependencies installed cleanly via
Debian-trixie-equivalent package names and it launched and rendered normally.

---

## Findings

### R4-T3-01 — PDF: last section/line orphaned onto an otherwise-blank second page

**Severity**: P1 · **Theme**: n/a (PDF export, all themes) · **Route**: Résumé PDF
export (`GET /api/resumes/{id}/pdf`) · **What**: fetched the base "PM" résumé through
all 8 templates plus its "Nomura" tailored copy. 6/8 templates (garamond,
garamond_alt, helvetica, montserrat, palatino, raleway) fit the identical content on
one page with comfortable bottom margin. Two templates — **inter** and
**traditional** — spill onto a near-blank second page: `inter` orphans just the final
line ("Bachelor of Science, Information Systems"); `traditional` orphans the entire
Education section (both entries) even though page 1 still has ~40% headroom below the
last visible line ("Languages: …"). The **tailored copy** (garamond — the template
that fits cleanly for the base résumé) also spills, orphaning the same lone last line,
because tailoring lengthens a few bullets just enough to cross the per-template page
budget. In every case the second page is otherwise empty (a single line or a short
section sitting alone). No orphan/widow control, no auto-shrink, and no
"keep the last section's heading with its first line" rule protects against this —
the renderer just lets the flow cross the page boundary wherever it falls.
**Screenshots**: `scratchpad/pdfs/png/base_inter_p1.png` + `_p2.png`,
`base_traditional_p1.png` + `_p2.png`, `tailored_p1.png` + `_p2.png` (rendered via
PyMuPDF from PDFs fetched through the live API; not copied into the repo — regenerate
via the API calls in the method note below if needed).
**Method**: `GET /api/resumes/{base_id}/pdf?template=<name>` for all 8 templates,
`GET /api/resumes/{tailored_id}/pdf`, `GET /api/cover-letters/{id}/pdf`; rendered to
PNG with PyMuPDF (`fitz`) at 2x scale.
**Root cause** (measured, all 4 base résumés × 8 templates + 10 tailored copies):
`@page { margin: 0 }` plus `.page { padding: <top> <side> }` means the page block's
bottom padding is *flow* content, so at a fragmentation boundary Chromium counts it
(0.35–0.5in = 25–36pt) against the last line and pushes that line onto page 2, even
when 20–30pt of physical paper is still free. The same applies to the final entry's
own trailing `margin-bottom`. Neither paints anything.
**Fix**: a `@media print` block in each of the 8 résumé templates (and, for parity,
the 8 cover-letter templates) that zeroes `.page`'s `padding-bottom`, zeroes the
trailing `margin-bottom` on the last `.entry`/`.edu-entry`/`.skill-line`, and adds
`break-after: avoid` to `.section-title` so a section heading is never stranded alone
at the foot of page 1. Screen/HTML preview is untouched (`@media print` only), and
page 1's ink is byte-identical for every case that already fit (verified by pixel
hash on the rendered PNGs).
**Result**: 8 of the 23 two-page cases collapse to a single page — base "PM" on
`inter`, base "PjM" on helvetica/montserrat/palatino/raleway, base "PjM FinTech" on
`garamond_alt`, and the tailored Nomura and Google copies. No case regressed from 1
to 2 pages; 4 cases move the trailing section heading onto page 2 with its entries
(intended, that is the `break-after` rule).
**Not fixable at template level** (reported rather than forced, per the fix brief):
15 cases where page 1's last ink already sits at y = 775–790 of the 792pt sheet, i.e.
the content genuinely exceeds one page by 10–160pt — every `TPgM` and most `PjM
FinTech` combinations, `traditional` throughout (the finding's "~40% headroom" reading
for base PM + traditional was wrong: page 1 ends at y=788/792, it is completely full),
and the Visa/Meta/Plaid2/Decagon/Brex tailored copies. Closing those needs a type-size,
spacing or content change, all out of scope here.
**Adjacent, pre-existing, not changed**: because `@page` margin is 0 and the only
vertical gutter is `.page`'s own padding, page 1 of a multi-page export prints to
within 2–17pt of the paper edge; most printers' unprintable margin will clip it. Fixing
that needs real `@page` margins, which would push *more* content to page 2.
**Status**: fixed for 8 of 23 two-page cases (template-level `@media print`
pagination block in all 8 résumé + 8 cover-letter templates); the remaining 15 are
genuinely over one page and are listed above as out of scope for a template-only fix

### R4-T3-02 — Win98: `.v2-scroll` custom scrollbar is not reskinned in any theme

**Severity**: P2 · **Theme**: Win98 (also present, less jarring, in every other theme)
· **Route**: any scrollable list/pane (Feed job list, Feed detail pane, Persona
sections, Settings nav) · **Appearance**: light+dark · **What**:
`frontend/src/v2/theme.css` lines 1105-1107 hard-code the app's custom scrollbar
thumb as a theme-independent 9px-wide, fully rounded (`border-radius:99px`) pill in
`--muted` on `--bg`, with no `data-theme="win98"` override. Win98's own geometry
contract is "0 radius everywhere... no exceptions found" (confirmed by
`proof-skins-B.md`'s corner-radius check) and the reference implementation
(jdan/98.css) uses a chunky 16-17px square scrollbar with a beveled 3D thumb, a
dithered track, and functional arrow buttons — one of the most recognizable Win98
signatures. A thin modern rounded scrollbar thumb sitting inside an otherwise flat,
square, bevelled Win98 window is a visible, contradictory paint. Not clipped or
unreadable, but a clear geometry-contract miss once bevels are fixed per the existing
P2 in `proof-skins-B.md`.
**Status**: fixed — `theme.css:169-171` (and the dark twin at `:391-393`) add `--scroll-thumb`
/ `--scroll-track` at today's values (`var(--muted)` on `transparent`), the win98
blocks re-point them to the chrome's own two greys (`#808080` thumb on `#c0c0c0`
trough), and `:1126-1134` now spells the bar twice: `scrollbar-width:thin` +
`scrollbar-color` for Chromium >= 121 and Firefox, with the `::-webkit-scrollbar*`
rules kept for older Chromium and re-pointed at the same two tokens (the thumb's
radius reads `--radius-control`, so it is square in win98). The bar deliberately
stays **slim** rather than the reference 16-17 px: `scrollbar-color` cannot express a
bevelled thumb, a dithered trough or arrow buttons in any engine, so a wide bar would
be a wide *flat* bar — further from 98.css, not closer. Expect a visible difference
in the default theme's screenshots too (native thin bar instead of the 9 px rounded
pill); it is the one deliberate default-theme repaint of this round and is logged in
`round-design/expected-R4.md` §C1.

### R4-T3-03 — Win98: hyperlink ink uses the navy accent, not the classic bright link blue

**Severity**: P3 · **Theme**: Win98 · **What**: `--link-ink`/`--link-ink-hover` are not
overridden in the win98 theme block, so they fall through to `var(--accent)` = `#000080`
(the same navy used for the title bar and selection fill). Genuine Windows 98 (and
jdan/98.css's `--link-blue`) uses a brighter `#0000ff` for hyperlinks, visually
distinct from the navy title bar/selection. Low-impact since the app has few bare
inline text links, but a legibility/authenticity nit worth a token (`--link-ink:#0000ff`
in the win98 block) if the depth pass goes ahead.
**Status**: fixed — `--link-ink:#0000ff` in both win98 blocks (`theme.css:950`, `:1002`), i.e.
98.css's own `--link-blue`, not the `#0000ee` the fix brief floated. `--link-ink-hover`
is `#000080`, the palette's existing navy: darkening on hover is the app's convention
everywhere else, and `#551a8b` was rejected because in this era that purple means
*visited*, so using it for hover would say something untrue. Contrast on the window
grey drops from 8.5:1 to 4.7:1, still over the 4.5 body floor. Scoped to the two
win98 blocks; no other theme moves.

### R4-T3-04 — Cross-browser (Chromium vs Firefox), Default theme: clean

**Severity**: — (informational) · **What**: captured all 13 routes at 1440/light in
both Chromium and Firefox (`v2-testing/artifacts/round4/t3/browsers/`). Compared
pixel crops of buttons (pill radius), toggle switches, dropdown carets, Recharts SVG
charts (Stats), and the Résumé/Persona form-field layouts. No material rendering
differences: button pill radius, switch track/knob colors, and chart rendering are
pixel-consistent; text reflow differs by a word here and there (expected sub-pixel
font-metric variance, e.g. one Persona bullet wraps a word earlier in Firefox) but no
clipping or broken layout in either engine. WebKit not tested — see the header note.
**Status**: closed (no action — recorded for completeness)

---

## Default theme

Dedicated fresh pass over all 26 screenshots (13 routes × light/dark, 1440) — the
default "warm paper" theme has no equivalent standalone review in
`proof-skins-B.md` (it only serves there as the S4b comparison baseline for the other
three skins). **Result: clean pass, no P1/P2.**

**R4-T3-07 — Default: Feed's selected-row tint is very subtle, no accent-bar
equivalent.** Severity P3 · `v2_feed` light · pixel-measured: selected row
`rgb(246,244,238)` vs unselected `rgb(252,251,247)` — a ~6/255 luminance delta with
no left accent bar or border (unlike the rail's own active-item treatment, which
does get a 2px accent bar). Easy to miss at a glance; may be an intentional quieter
row-selection convention for this skin rather than a bug. **Status**: closed
(decided) — the Feed's selected-row wash is a settled decision, not an open
question: `147ab7a` ("selected row wash reverted to --surface-2") and `e91930c`
before it moved this tint deliberately, and the round-4 fix brief lists it among the
hand-tuned values not to revert. No change.

No other issues anywhere: ScoreRing plain-ring variant confirmed clean (no clipping)
at both Feed-row and Résumé-detail sizes in both modes; rail active-tile accent bar,
tags/chips/ATS badges, dashed bands, toggle switches (verified on/off states via
zoom crop), and the newer glyph-badged Run/Test row controls, the unified small
row-control cluster, and Persona-import UI all render correctly and legibly across
every route and mode.

## Board theme (restored today from an older palette)

Reviewed by a dedicated pass over all 26 screenshots (13 routes × light/dark, 1440).
**Result: clean pass, no P1/P2.** Every semantic token resolves — no magenta,
transparent, or black-on-black anywhere; dark mode correctly re-derives its own
darker tokens rather than reusing light-mode hex (pixel-verified: rail dark-green
`rgb(30,46,38)` in dark vs `rgb(40,67,54)` in light — genuinely distinct blocks, not a
rendering illusion). Rail active tile (forest-green fill + gold left accent bar),
plain-ring ScoreRing (board is a palette-only skin like default/editorial, not a deep
retheme, so ring/radius/caps geometry is inherited unchanged), tags/chips/ATS
badges, dashed bands, switches and dropdowns all read as properly themed controls in
both modes across all 13 routes. The only items worth a footnote — the fixed blue
"Applied" status tag and the terracotta "Rejected" pill — are shared, cross-theme
semantic status colors used identically in every theme, not a board-specific leak.

---

## Cobalt theme

Reviewed all 26 screenshots (13 routes × light/dark, 1440) with fresh eyes against the
existing `proof-skins-B.md` cobalt findings, specifically checking whether the
subsequent "field hover / pressed / filled-button hover on, small row control
unified" commit regressed anything. It did not.

**R4-T3-05 — Cobalt: Feed detail-pane "Tailor résumé" button still has no drop
shadow.** Severity P2 · confirms (does not newly find) the one defect
`proof-skins-B.md` already logged: every other primary CTA checked (Score N unscored
jobs, Regenerate…, Tailor for a job…, Review N changes, + New résumé, + Log
application, even the Toast-lab's Fire all) correctly shows cobalt's soft blue-tinted
button shadow; the Feed sticky-sub-header's "Tailor résumé" alone renders flat, in
both light and dark. Unfixed since the prior pass. **Status**: closed — does not
reproduce in the source at HEAD, and neither of the fix brief's two hypotheses holds.
(1) The `Button` route was **not** reverted: `JobFeed.jsx:1128-1131` still renders the
control as `<Button size="sm">` with the S5 comment intact, and `git diff
18a775a..HEAD -- frontend/src/v2/JobFeed.jsx` shows the only change to those lines is
the comment being shortened by the comment-trim commit `96e1c16`. (2) `--btn-shadow`
is **not** size-gated: `ui.jsx:172` writes `boxShadow: off ? 'none' :
'var(--btn-shadow)'` once, before `...s` (the size map, which carries only
height/fontSize/padding), before `look.rest` (background + colour only) and before the
call site's `style` (height/padding/lineHeight only) — nothing downstream can
overwrite it, at any size. Cobalt sets `--btn-shadow:0 1px 2px rgba(45,91,227,.30)` in
both of its blocks, and `verify-S5.md` §5 recorded this exact button fixed with a
pixel diff that found `feed` the only changed cobalt route, in the detail-header
bbox. The most likely explanation for the round-4 sighting is a capture taken against
a bundle predating `18a775a`; **re-capture cobalt `/v2/feed` on the rebuilt bundle
before reopening.** No code change — there was nothing at the primitive to fix.

No new P1/P2 surfaced. One P3 noted for completeness, explicitly not cobalt-specific:
the Companies search input's placeholder text ("Search name, alias, URL or ATS
ID…") clips mid-word to "…or ATS" at 1440 — plain input-width overflow, present
identically in the default baseline, not a skin paint issue.

No stuck/incorrect hover, pressed, or focus-ring state was found anywhere from the
recent broad hover/pressed-state change — every control's resting-state paint matches
expectations in both modes. Rail active tile, pill ScoreRing (40×44, Plex Mono
numeral, "FIT" caps label), the square `#eef3fe` + 3px accent-edge row selection, 8px
control radius, and cobalt-tinted tags/chips all remain correct and unaffected by
intervening commits.

**Visual character** (for the Cobalt/SaaS sameness comparison): a vivid, high-saturation
royal blue accent (~`#2d5be3`) applied narrowly to primary CTAs/links/focus/row-accent,
against a cool near-white (light) / near-black-navy (dark) ground — not warm paper.
IBM Plex Sans at a confident ~600 weight for headings, uppercase `.06em` caps for
labels, monospace for numeric "readouts" (scores, model IDs). Elevation is sparse and
singular — only the one primary action per screen gets a shadow; every field,
secondary button, card and row is flat/bordered/shadowless, so cobalt reads as
graphic/flat rather than soft. Row selection is a hard-edged, square-cornered wash
with a crisp accent bar. Overall vibe: closer to an engineering console/trading
terminal than a soft consumer SaaS product.

## SaaS theme

Reviewed all 26 screenshots (13 routes × light/dark, 1440) against `proof-skins-B.md`'s
prior clean pass and specifically hunted for regressions from the intervening
hover/pressed/glyph-badge commits. None found.

**R4-T3-06 — SaaS: "Toast lab" H1 uses an off-spec lighter-weight font.** Severity P3
· `v2_toasts` light+dark · the page H1 renders in a visibly thinner, different
typeface than every other route's ~750-weight title (compare against "Stats").
`proof-skins-B.md` already flagged the identical defect for cobalt but did not check
saas — it's present there too. Internal, explicitly-temporary dev page slated for
deletion per `HANDOVER.md` — low stakes. **Status**: fixed — and it was **not**
the `PageTitle` primitive, which reads `var(--title-weight)` correctly (saas 750,
cobalt 600, base 400). `design-base/ToastLab.jsx:29` hand-rolled its own `<h1>` with
`fontWeight: 400` pinned inline and never used the primitive; it is now
`<PageTitle>Toast lab</PageTitle>`. `design-base/` is git-ignored, so this is a
local-only change to a page `HANDOVER.md` already marks for deletion; `/v2/toasts` is
not in `stylecrawl.py`'s routes, so the gate does not see it.

No P1/P2. Confirmed: the ScoreRing bar variant (mono numeral + short horizontal
track) renders correctly and unclipped everywhere; row selection is the rounded
accent-soft wash with no left edge bar (correctly distinct from cobalt); the rail's
filled active tile is legible in both modes; the soft-shadow-on-every-bordered-control
elevation signature (SaaS's headline feature) remains intact everywhere, including on
the exact Feed sticky-header "Tailor résumé" button that regressed in cobalt — it did
**not** regress here.

**Visual character** (for the sameness comparison): a cool, restrained
"enterprise-dashboard" aesthetic — a moderately saturated, cooler blue than cobalt's
vivid royal blue, applied to the same role set (primary buttons, rail tile, selected
pills, links, row wash). Near-white/cool-gray light surfaces, near-black-navy dark
surfaces, almost no warm tones except the incidental cream Q&A-bank card and
data-driven score-tier ambers. Bold, dense system-sans titles at a heavier weight
than body text; small tracked uppercase caps for labels — a "spec sheet" feel.
Shadows are visible and consistent everywhere — every bordered control (fields,
dropdowns, pills, buttons, cards) carries a soft 1px drop shadow, stronger on
primary — the single most defining SaaS signature, giving a gently "lifted" feel
rather than cobalt's flat/graphic one. Corners are uniformly, moderately rounded
(8-10px) — softer than cobalt's mixed square-row/round-field look. The rail stays a
fixed dark navy regardless of the app's light/dark appearance. Overall vibe: polished,
businesslike B2B-analytics-tool default, favoring legibility/structure over
personality.

## Win98 theme

Reviewed all 26 screenshots twice — once directly (pixel-cropping and channel-level
border scans) and once via a dedicated agent pass that additionally cross-referenced
the current source (`ui.jsx`, `theme.css`, and per-screen files at HEAD `61d00ef`) —
to verify each of the four defects `proof-skins-B.md` logged and commit `18a775a`
("Skins proof fixes: win98 selected-row ink, bevels scoped to win98, label
case/tracking scale token, engraved disabled ink…") claimed to fix. Two are cleanly
fixed; the other two are fixed at the primitive level but leave several hand-rolled
call sites that never adopted the primitive, still showing the original defect.

- **Row-selection ink — VERIFIED FIXED.** Feed's selected row (light: navy `#000080`
  fill; dark: `rgb(43,58,92)` = `#2b3a5c`) shows white title/company/meta text in
  both modes — pixel-sampled directly, no black-on-navy anywhere.
- **Disabled button ink — VERIFIED FIXED.** Cover Letters' empty-form "Generate cover
  letter" button: ink and background are the *same* `rgb(128,128,128)`, legible only
  via a 1px near-white emboss offset on each glyph — the authentic win98
  "ghost/engraved" disabled look, pixel-confirmed, not a flat muted-grey disable.
- **Bevels — fixed at the primitive level, several hand-rolled controls never got the
  hook.** `Pill`, `Select`, and `Input`/`Textarea` *do* correctly bevel (confirmed on
  Companies'/Feed's search boxes, Settings' dropdowns, Persona's fields, Run/Test
  pills, the Applied/Interview/Offer/Rejected segmented control, and Settings'
  "Manage…" button — light top/left, dark bottom/right, both modes). But channel-level
  border scans of Feed's action buttons show the shared `Button` primitive is not
  bevelled at all: the secondary "Open" and the "…" icon button get a **flat, single
  uniform `#404040` stroke** on every edge (scanned row: `(192,192,192)` pane → sharp
  jump straight to `(64,64,64)` → `(192,192,192)` interior, identical value on both the
  left and right edge), and the primary "Tailor résumé" button has **no border pixel
  at all** — the fill jumps directly from the pane grey to solid navy with zero
  transition pixels, not even a flat stroke. Two more hand-rolled sites bypass the
  bevelled primitives entirely: Settings' own "Search settings…" field (`Settings.jsx`
  hand-rolls a `.v2-fieldwrap` div with a plain `border:1px solid var(--edge)` instead
  of using `SearchInput`) and the PDF-preview `Template`/`Paper` `ToolbarTrigger`
  pickers (excluded from the `v2-inset` hook by the primitive's own code comment, on
  every theme, not just win98). **R4-T3-09, P2, Status**: fixed at the primitive for the
  `Button` half — `ui.jsx:168-173`: `Button` now carries `v2-raised` on its className, the same bevel
  hook `Pill`, `IconButton` (bordered), `Chip`, `Menu`, `ModalPanel`, `Segmented` and
  `ToastCard` already wear, on **every** variant (primary, ai, danger, secondary,
  ghost) and on the disabled state too — a win98 button is still a button. The rule
  is `[data-theme="win98"]`-scoped, so no other theme moves a pixel; the one gate
  consequence is that every `Button`'s crawl key gains `.v2-raised` (logged in
  `round-design/expected-R4.md` §B1). The other two call sites are **deferred
  (v2.1)** and deliberately so: Settings' hand-rolled `.v2-fieldwrap` search field
  would need `SearchInput`'s own geometry (h32, r99, `--search-bg`, a 29 px left
  inset for the glyph) or the `v2-inset` hook, and the hook's D-06 hover rule is
  `!important` in *every* theme — either route repaints the default theme, which this
  round's brief does not authorise; `ToolbarTrigger`'s exclusion from `v2-inset` is
  D-06's own recorded carve-out (the hook's `!important` hover would beat the
  `v2-bdc` accent hover D-13 gives it), so changing it is a decision, not a fix.
- **Label case/tracking — fixed in the four spots `proof-skins-B.md` sampled, the same
  hardcoded-uppercase pattern persists unfixed in at least four more.** Sentence case
  confirmed correct now on: the rail's group headers ("Find"/"Apply"/"You"), the
  Résumé Base/Tailored badge, and the toast-lab kind labels. Still forced uppercase,
  each for its own distinct code reason: **Settings' left-nav group headers**
  ("GENERAL"/"AI"/"PIPELINE"/"INTEGRATIONS"/"SYSTEM" — `Settings.jsx` passes literal
  uppercase *strings* like `'GENERAL'` to `Label`, so `--label-case:none` has nothing
  to lowercase); **the Cover Letters stage badge** ("DRAFT" — `CoverLetterEditor.jsx`
  calls `.toUpperCase()` on the stage value; the fix commit updated this span's
  tracking/case *CSS* but never touched the string transform feeding it, so the fix
  is cosmetically applied but structurally inert); **Feed's unscored-job "SCORE" chip**
  (`JobFeed.jsx` hardcodes inline `textTransform:'uppercase'`, never reads
  `--label-case`); and **Stats' LLM-cost table head** ("PURPOSE MODEL CALLS COST
  CACHE" — `Stats.jsx` hand-rolls a `COL` style object with its own
  `textTransform:'uppercase'` instead of using the `TableHead` primitive, which the
  *same page*'s "Schedules" table two cards below uses correctly and renders in
  proper sentence case). **R4-T3-08, P2, Status**: fixed — one sweep across all
  four, each repaired at whatever was actually bypassing the token. **Settings'
  left-nav headers** (`Settings.jsx:341`, `:406`, `:426`, `:476`): the literal strings are now
  sentence case (`'General'`, `'Pipeline'`, `'Integrations'`, `'System'`; `'AI'` was
  already right), so `Label`'s own `textTransform: var(--label-case)` does the work.
  **Cover Letters stage badge** (`CoverLetterEditor.jsx:335`): `.toUpperCase()` on
  the stage is replaced by capitalising the first letter only, so the CSS fix already
  on that span is no longer inert. **Feed's SCORE chip** (`JobFeed.jsx:1050`): inline
  `textTransform:'uppercase'` becomes `var(--label-case)`. **Stats' LLM-cost head**
  (`Stats.jsx:90`): the hand-rolled `COL` object reads `var(--label-case)` and
  `var(--label-tracking-strip)` instead of its own literals (its box stays hand-rolled
  — it is a 22 px / 9 px strip, below `TableHead`'s 28 px / 9.5 px, inside a
  scroll-gutter head — but it is now on the tokens). Every one of the four still
  renders upper-case in the default theme, so no pixel moves there; two of them change
  their crawl *key*, because the key embeds the first text node (see
  `round-design/expected-R4.md` §B2/§B3).

**R4-T3-10 — Win98 dark mode: selected-row background barely distinguishable from
the unselected desktop tone.** Severity P3 · `v2_feed` dark · pixel-sampled: selected
row `rgb(43,58,92)` vs. unselected-row/desktop `rgb(31,43,58)` — both dark
navy-adjacent tones with a small luminance delta. Text is still legible (white-on-navy,
per the fix above), but at a glance it's hard to tell which row is active in dark
mode, unlike light mode's much higher-contrast teal-desktop vs. navy-selection split.
**Status**: fixed — one token, in the win98 **dark** block only: `--row-selected` is
`#4a6a99` instead of `var(--accent)` (`theme.css:992`) (`#2b3a5c`). That is the light stop of this
palette's own dark title-bar gradient, so it still reads as "selection blue" in
context, and the selected row now stands ~2.65:1 off the desktop tone (`#1f2b3a`)
where it was ~1.28:1. `--row-selected-ink` stays `--accent-ink` (white), which is
5.3:1 on the new fill. Nothing else — `--accent`, the rail's active tile and win98
light are all untouched.

Confirmed clean: ASCII ScoreRing renders correctly with no clipping/wrapping in
every place it appears (Feed rows, Feed detail, Résumé tailored-copy header — "40
[...]"/"77 [...]"); zero corner radius holds everywhere checked (cards, rows, pills,
buttons, modals, switches, table strips); the teal(light)/navy(dark) desktop ground
shows correctly behind the grey window chrome on every list-style route (the
full-bleed two-pane detail/editor routes have no gutter by layout design, not a lost
desktop); the rail base stays grey with only the active item's own tile filling navy;
the Switch primitive is correctly reskinned as a flat square (track+knob, no radius)
with win98's own navy-on/grey-off colors rather than inheriting another theme's pill
shape; no regression to any of this from the later hover/pressed-state commit — win98
already carries its own `--input-border-hover:var(--bevel-inset-color)` override to
avoid an invalid-color fallback from that commit's universal hover rule. The custom
`.v2-scroll` thumb from R4-T3-02 was not visibly triggered in any of these 26 static
captures (no pane scrolled far enough to show a thumb) — that finding rests on the
`theme.css` source, not a live sighting, and stands either way.

**Verdict**: Win98 has visibly improved since `proof-skins-B.md`'s original pass —
the selected-row contrast failure (its one P1) and the disabled-ink treatment are
both genuinely and fully fixed. The other two fixes both landed correctly at the
primitive level (`Pill`/`Select`/`Input` do bevel; four of the originally-sampled
label spots are sentence case) but stop there — a small, findable set of hand-rolled
call sites (the `Button` primitive in every variant, Settings' own search field, and
four more hardcoded-uppercase labels across four different files) never adopted the
fixed primitive and still show the pre-fix look. None of it is unreadable (no new
P1), but the pattern is systemic enough — the same two defect classes recurring in
new call sites — to warrant a proper sweep for remaining hardcoded
`textTransform`/`.toUpperCase()` and un-bevelled bordered controls, not one-off
patches per site. Geometry fundamentals (zero radius, desktop/window split, ASCII
ScoreRing, Switch reskin) remain solid throughout.

---

## Contrast tables (WCAG, resolved from `frontend/src/v2/theme.css`'s actual cascade)

Method: parsed `theme.css`, replicated the real CSS cascade for each
`(data-theme, data-appearance)` combination (base `.jn-v2` → `[data-appearance="dark"]`
→ `[data-theme="X"]` → `[data-theme="X"][data-appearance="dark"]`, later wins on a
tie per source order/specificity — the same four-selector stack `proof-skins-A.md`
used), followed `var()` chains to concrete hex, and computed WCAG relative-luminance
contrast ratios. Same pairs as `proof-skins-A.md` §3, plus the Board theme (not
covered there). Default/Cobalt/SaaS/Win98 numbers match `proof-skins-A.md` almost
exactly (confirms the CSS hasn't drifted from that audit); Board is new.

Body-text pairs (text/text-2/muted on bg/surface) flagged below 4.5:1; every other
pair (large text/UI chrome) flagged below 3:1.

### default

| pair | light | dark |
|---|---:|---:|
| text / bg | 16.82 | 11.82 |
| text-2 / bg | 7.40 | 10.10 |
| muted / bg | 5.33 | 6.86 |
| text / surface | 17.41 | 10.64 |
| accent-ink / accent | 6.11 | 8.55 |
| accent / bg | 5.90 | 7.90 |
| good / bg · good / accent-soft | 5.90 · 5.32 | 7.90 · 6.37 |
| warn / bg · warn / warn-soft | 5.20 · 4.68 | 7.32 · 6.80 |
| bad / bg · bad / bad-soft | 6.58 · 5.89 | 6.39 · 6.27 |
| rail-ink / rail | 14.53 | 17.28 |
| tag-neutral/-accent/-good/-warn/-bad ink on ground | 6.96 / 5.32 / 5.32 / 4.68 / 5.89 | 7.95 / 6.37 / 6.37 / 6.80 / 6.27 |

All pass.

### board (new — not in proof-skins-A)

| pair | light | dark |
|---|---:|---:|
| text / bg | 15.04 | 11.38 |
| text-2 / bg | 8.80 | 9.66 |
| muted / bg | 6.72 | 6.58 |
| text / surface | 16.11 | 10.64 |
| accent-ink / accent | 8.26 | 7.58 |
| accent / bg | 8.56 | 7.58 |
| good / bg · good / accent-soft | 8.56 · 7.78 | 7.58 · 6.11 |
| warn / bg · warn / warn-soft | 5.52 · 4.96 | 7.93 · 6.80 |
| bad / bg · bad / bad-soft | 7.55 · 6.39 | 6.93 · 6.27 |
| rail-ink / rail | 10.61 | 13.91 |
| tag-neutral/-accent/-good/-warn/-bad ink on ground | 8.18 / 7.78 / 7.78 / 4.96 / 6.39 | 8.37 / 6.11 / 6.11 / 6.80 / 6.27 |

All pass — Board is the best-margined of the five themes (its `warn/bg` 5.52/7.93 and
`muted/bg` 6.72/6.58 clear the floor more comfortably than default's own 5.20/5.33).
No accessibility regression from the restoration.

### cobalt

| pair | light | dark |
|---|---:|---:|
| text / bg | 16.28 | 15.71 |
| text-2 / bg | 5.62 | 9.69 |
| muted / bg | **4.07** ⚠ | 6.28 |
| text / surface | 17.76 | 14.76 |
| accent-ink / accent | 5.65 | 3.90 |
| accent / bg | 5.18 | 4.84 |
| good / bg · good / accent-soft | 4.94 · 4.60 | 10.11 · 7.48 |
| warn / bg · warn / warn-soft | 4.37 · 4.26 | 11.28 · 9.20 |
| bad / bg · bad / bad-soft | 4.85 · 4.57 | 7.91 · 6.49 |
| rail-ink / rail | 17.76 | 19.57 |
| tag-neutral/-accent/-good/-warn/-bad ink on ground | 5.77 / 4.82 / 4.60 / 4.26† / 4.57 | 8.45 / 3.58†† / 7.48 / 9.20 / 6.49 |

Matches `proof-skins-A.md`'s numbers (unchanged since that audit). `muted/bg` at
4.07 (light) is UI-weight text (labels/helper text, not body copy) so it clears the
3:1 UI floor but misses the stricter 4.5 body floor — same pre-existing, not-new gap
`proof-skins-A.md` already logged. † / †† below 4.5 but above 3:1 — same borderline
tag cells `proof-skins-A.md` already flagged as inherited, not new.

### saas

| pair | light | dark |
|---|---:|---:|
| text / bg | 16.98 | 16.12 |
| text-2 / bg | 7.23 | 12.04 |
| muted / bg | 4.63 | 6.99 |
| text / surface | 17.74 | 13.34 |
| accent-ink / accent | 6.70 | 7.36 |
| accent / bg | 6.41 | 6.98 |
| good / bg · good / accent-soft | 4.80 · 4.61 | 10.18 · 6.60 |
| warn / bg · warn / warn-soft | 4.81 · 4.84 | 10.63 · 10.01 |
| bad / bg · bad / bad-soft | 6.19 · 5.91 | 6.41 · 6.25 |
| rail-ink / rail | 17.85 | 19.57 |
| tag-neutral/-accent/-good/-warn/-bad ink on ground | 6.87 / 6.16 / 4.61 / 4.84 / 5.91 | 8.76 / 4.52 / 6.60 / 10.01 / 6.25 |

Matches `proof-skins-A.md`. All pairs pass (dark `tag-accent` 4.52 is the closest,
just above 4.5).

### win98

| pair | light | dark |
|---|---:|---:|
| text / bg | **4.40** ⚠ | **1.46** ⚠ |
| text-2 / bg | **4.40** ⚠ | **1.46** ⚠ |
| muted / bg | **2.17** ⚠ | **1.38** ⚠ |
| text / surface | 11.54 | 11.54 |
| accent-ink / accent | 16.01 | 11.28 |
| accent / bg | 3.35 | **1.27** ⚠ |
| good / bg · good / accent-soft | **1.65** ⚠ · 4.32 | **1.93** ⚠ · 4.09 |
| warn / bg · warn / warn-soft | **1.59** ⚠ · 7.38 | **2.25** ⚠ · 6.21 |
| bad / bg · bad / bad-soft | **2.29** ⚠ · 7.92 | **1.31** ⚠ · 7.92 |
| rail-ink / rail | 11.54 | 11.54 |
| tag-neutral/-accent/-good/-warn/-bad ink on ground | 21.00 / 8.80 / 4.32† / 7.38 / 7.92 | 21.00 / 6.20 / 4.09† / 6.21 / 7.92 |

Matches `proof-skins-A.md` exactly. Every flag is on `--bg` (win98's `--bg` is the
*desktop* teal/navy behind the window, never a text ground — `--head-bg-page` and
`--chip-bg` are re-pointed to `--surface` specifically so text never actually paints on
it). Pre-documented, accepted trade-off, not a new finding.

---

## Design brief input — Cobalt vs SaaS sameness

Token-level comparison plus independent confirmation from the two dedicated theme
reviews above (each reviewer wrote their "visual character" paragraph without
seeing the other's, specifically to cross-check this list). Concretely what reads
the same at a glance:

1. **Same blue family for the primary accent.** Cobalt `#2d5be3` vs SaaS `#1d4ed8` —
   both a mid-saturation, mid-lightness blue in the same 15-20° hue band. Applied to
   the identical set of roles in both (primary buttons, links, focus ring, tag-accent,
   ScoreRing "hi" band), so the one color a user's eye anchors on is nearly identical
   between the two skins.
2. **Same corner-radius language.** Both use 8px on fields/controls/cells
   (`--radius-field/-control/-cell:8`) and near-identical 9-10px on cards/menus/modals
   (cobalt 9/9/9, saas 10/10/12) — a gap too small to read as different at normal
   screen distance. Radius is usually the fastest way a user distinguishes two
   "rounded" themes, and here it doesn't.
3. **Same caps-label treatment.** Both: `.06em` tracking, uppercase, 600-700 weight
   section headers/labels — a 100-weight difference that reads identically at
   10-11px.
4. **Same typographic register**, different files: Cobalt loads IBM Plex Sans, SaaS
   loads the system-ui/Segoe/Helvetica stack — both are clean grotesque sans faces
   with similar x-height and no serif/mono anywhere in a heading slot, so the "voice"
   of the two themes reads the same even though the font files differ.
5. **Same "filled tile" rail-active silhouette** (vs. default/board's thin left bar or
   win98's flat navy fill spanning the row) — cobalt's tile is a fixed dark navy
   `#2c3442`, saas's is the live accent color, but both render as a rounded, inset,
   colored block behind the active nav item at the same `0 10px` inset.
6. **Same "no bevel, soft/no elevation" bucket.** Neither theme touches
   `--bevel-*` (both `none`); cobalt's shadow is a colored soft glow, saas's is a
   neutral soft glow, but both sit in the same "modern SaaS elevation" visual
   register, clearly apart from win98's raw bevels or default/board's flat paper
   cards.
7. **Both ScoreRing variants are non-circular, compact numeral badges** (cobalt: pill
   40×44 tile; saas: bar, numeral + 32×3 track) — different literal shape, same "not a
   ring, a small mono-numeral badge" silhouette at row scale in Feed, where most users
   actually look at it.
8. **Neither customizes the disabled state** — both fall through to `--muted` for
   `--disabled-ink`, same as default; no skin-specific "this control is off" signal.

The one genuine geometric difference a user could spot side by side — cobalt's
**square selected row** (radius 0, `#eef3fe` wash + inset 3px accent edge) vs saas's
**rounded selected row** (radius 8, plain `--accent-soft` wash, no edge) — is confirmed
against the actual screenshots: both dedicated reviews independently verified their
own theme's row-selection shape (cobalt: "hard-edged, square-cornered wash with a
crisp accent bar"; saas: "rounded accent-soft wash, no left edge accent bar"). It is
the single most reliable visual tell between the two skins — everything else in the
sameness list above reads the same at a glance.

---

## Design brief input — Win98 depth deltas vs jdan/98.css

Reference values pulled from the canonical `jdan/98.css` stylesheet (`:root` block):
`--surface:#c0c0c0`, `--button-highlight:#ffffff`, `--button-face:#dfdfdf`,
`--button-shadow:#808080`, `--window-frame:#0a0a0a`, `--dialog-blue:#000080`,
`--dialog-blue-light:#1084d0`, `--link-blue:#0000ff`; chrome font
`"Pixelated MS Sans Serif"` (a real bitmap-derived webfont) at 11px for controls vs
body copy in Arial 12px, with `-webkit-font-smoothing:none`; scrollbars 16-17px wide
with a beveled thumb, dithered track and functional arrow buttons; checkboxes/radios
are bitmap-derived SVG glyphs, not CSS-drawn shapes.

| Aspect | jdan/98.css reference | JobNavigator win98 | Delta |
|---|---|---|---|
| Window chrome grey | `#c0c0c0` | `--surface:#c0c0c0` | Match |
| Title bar gradient | `linear-gradient(90deg,#000080,#1084d0)` | identical (`--title-bar`) | Match |
| Selection/accent blue | `#000080` | `--accent:#000080` | Match |
| Bevel tone count | **4-tone**: white highlight → `#dfdfdf` face → `#808080` shadow → `#0a0a0a` frame (a double-inset box-shadow stack) | **2-tone**: `#ffffff` / `#404040` only (`--bevel-raised-color`) | Real depth loss even once the existing "bevels never render" P2 (`proof-skins-B.md`) is fixed — a corrected cobalt/saas-style 2-tone bevel still won't read as deep as the authentic 4-step recess |
| Darkest bevel/frame line | `#0a0a0a` (near-black) | `#404040` (mid-grey, reused from `--edge`/`--muted`) | Corners will look soft, not crisp |
| Hyperlink ink | `#0000ff` (bright, distinct from title-bar navy) | falls through to `--accent` = `#000080` (same navy as the title bar/selection) | R4-T3-03 above |
| Chrome font | Real pixel-hinted bitmap webfont, anti-aliasing off | `Tahoma,'MS Sans Serif',Verdana,system-ui` — a live font-fallback stack, anti-aliased | The single biggest authenticity gap: no build ships a Windows bitmap font, so the app can only ever approximate the crisp pixel look with a smoothed system font |
| Scrollbar | Custom 16-17px, beveled 3D thumb, dithered track, arrow buttons | Generic 9px rounded pill thumb, not reskinned at all | R4-T3-02 above — the single most visible missing signature |
| Checkbox/radio glyphs | Bitmap-derived SVG dot/checkmark | Shared CSS-drawn Check/Radio primitive | Lower shape fidelity, not checked pixel-by-pixel this pass |
| Window frame / floating-window border | `.window`'s own raised/sunken outer border, visually separating it from the desktop | Not built — `Skins handoff.md` §5 lists "Floating windows (W7) and the `_ □ ×` bar" as a known, already-acknowledged gap | Not new, but still open |
| Bevel coverage by component | Every bordered control in 98.css (buttons, fields, wells) gets the raised/sunken treatment uniformly | Confirmed correctly bevelled: `Pill`, `Select`, `Input`/`Textarea` (search boxes, dropdowns, form fields, segmented control). Confirmed **not** bevelled: the `Button` primitive in any variant (channel-scanned — secondary/icon buttons get a flat single-tone stroke, the primary filled variant has no border pixel at all), Settings' own hand-rolled search field, and the `ToolbarTrigger` (PDF Template/Paper pickers, excluded from the bevel hook by its own code comment on every theme) — see R4-T3-09 | `Button` is one of the most common control types in the app; a flat/border-less button reads as inert or broken in a UI whose whole idiom is "raised = clickable" |
| Menus/dropdowns | No literal 98.css menu component (native `<select>` popups); classic win98 menus are flat/square/hard-shadowed | `Select`'s closed trigger is correctly bevelled (see row above); the `Menu` primitive's own popover tokens (`--radius-menu:0`, `--bw-panel:2`, hard offset `--shadow-menu`) were not directly exercised by an open menu/popover in any of the 26 static captures | The closed-trigger look is confirmed correct; the open popover's own look remains unconfirmed this pass |
| Caps-label / sentence-case coverage | n/a (98.css uses sentence case throughout by convention, no forced-caps mechanism to bypass) | Fixed in 4 sampled spots (rail headers, Base/Tailored badge, toast-lab labels) but the identical hardcoded-uppercase pattern recurs, unfixed, in 4 more: Settings' left-nav headers (literal uppercase strings), the Cover Letters stage badge (`.toUpperCase()`), Feed's "SCORE" chip (inline `textTransform`), and Stats' LLM-cost table head (a hand-rolled style object bypassing the `TableHead` primitive) — see R4-T3-08 | A recurring pattern across 4 different files/components, not isolated misses — suggests a codebase-wide sweep for hardcoded case transforms rather than four separate patches |

---

## Method notes

- Captures: `docker compose exec backend` running `v2-testing/tools/shots.py T3_<theme>
  --theme <theme>` for each of default/board/cobalt/saas/win98 (52 shots each — 13
  routes × light/dark × 1440/1024), copied to
  `v2-testing/artifacts/round4/t3/<theme>/`.
- Browsers: Firefox 132 and WebKit 2092 installed via `python -m playwright install
  firefox webkit`; `install-deps` failed (Debian trixie package names don't match the
  ubuntu20.04-fallback dependency list) but the individual packages resolved under
  their trixie-equivalent names (`libgtk-3-0t64`, `libicu76`, `libvpx9`, …) except the
  exact old sonames WebKit needs (`libicudata.so.66`, `libjpeg.so.8`) which trixie no
  longer ships under any package name — WebKit could not be launched. Firefox worked
  after that same package pass.
- PDFs: fetched via the live API with `X-API-Key`, rendered to PNG with PyMuPDF
  (`fitz`), reviewed page-by-page.
- Contrast: `contrast.py` (scratchpad) parses `theme.css`, replicates the real
  cascade per `(theme, appearance)`, resolves `var()` chains, computes WCAG ratios.
- Scheduler paused for the capture window (`scrape_interval_minutes`/
  `email_check_interval_minutes` set to 0) and restored to 3500/180 afterward,
  verified with a GET.
