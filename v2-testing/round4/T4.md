# Round 4 · T4 — soak

Dropped on 2026-09-05 by the user (finish the round the same day). The scheduler has been running live on real sources throughout rounds 3–4; the T2b 20-minute session (timers, DOM, heap flat) and the T1 failure-injection tests stand in for it. A day-scale log review can be run later with the recipe below if wanted:

`compose logs backend --since 24h`, `GET /api/monitor/history`, `/api/stats/llm-costs?days=1`, job/application counts, DB size; findings `R4-T4-nn`.

**Status**: closed (dropped by decision, 2026-09-05)
