# Open P3 / P4 findings — consolidated

Generated from `stage3/*.md` and `FINDINGS.md`. Design-related rows also appear in `DECISIONS-design.md` (marked *design*). Full entries live in the per-screen reports.


**Open: P3 97 · P4 87 · total 184**


## applications (15)

| ID | Sev | Kind | Title | Proposed fix | Status |
|---|---|---|---|---|---|
| APPS-09 | P3 | design | The detail body collapses to an unusable 20 px at 1024 px width | drop the history rail below the content (or hide it) under a width threshold, and/or let the list pane shrink… | needs decision — the design has no narrow breakpoint to match. |
| APPS-10 | P3 | design | `.v2-hover-accent`'s colour half never fires (Prep modal ✕) — cross-screen | — | needs decision — coordinator-level: add `!important` to `.v2-hover-ac… |
| APPS-11 | P3 | functional | Add-interview has no in-flight guard — a double click creates two interviews | a `busy` ref/state around the POST, plus `opacity .6` + `onClick={busy?undefined:addInterview}` mirroring `:6… | needs decision (small, but touches the same block as APPS-06/12). |
| APPS-12 | P3 | functional | A completely blank interview form creates an "Interview / Unscheduled" card | disable "Add interview" while `!intWhat.trim() && !intWhen`. | needs decision. |
| APPS-13 | P3 | design | Interview ✕ deletes with no confirm and no undo | `window.confirm` (matching `:171`) or an undo toast. | needs decision. |
| APPS-14 | P3 | functional | A filtered-out application stays open in the detail pane with no indication | either keep it (defensible — you don't lose your place) but add a hint line, or blank the detail when `!visib… | needs decision. |
| APPS-15 | P3 | functional | A hand-logged application's history reads "Discovered via a company scrape" | map `direct → 'the Log application form'` in `srcLabel`, or stamp a distinct `source="manual"` in `create_app… | needs decision. |
| APPS-16 | P3 | design | Design deviations in layout and copy | — | needs decision: keep code (consistency) or match design? — except (j)… |
| APPS-17 | P3 | design | The Log modal demands a URL although its own copy says it is for off-app applic… | either accept a blank URL (synthesise `manual://{company}/{title}` for the external_id) or say so in the fiel… | needs decision. |
| APPS-18 | P4 | functional | "Cached" never appears for a freshly logged application until a later refetch | a single delayed `load()` (~5 s) after a save, or an optimistic "caching…" chip. | needs decision. |
| APPS-19 | P4 | functional | The rail "Applications" badge is never refreshed by this screen | a `jn:counts-changed` window event the shell listens for, dispatched after log/delete. | needs decision (shell-level, affects every screen). |
| APPS-20 | P4 | design | Selected row and hovered row are the same colour | — | needs decision: keep design parity, or give the selected row a left a… |
| APPS-21 | P4 | functional | "Applied on" is a UTC date sent as UTC midnight | build the default from local parts and send `new Date(when + 'T12:00:00')` (or keep the date-only string and … | needs decision. |
| APPS-22 | P4 | functional | Smaller items (grouped) | — | log only. |
| APPS-23 | P4 | design | The "Cached" link bypasses the API-key header (works only via the session cooki… | — | log only — no live defect in this deployment. |

## companies (22)

| ID | Sev | Kind | Title | Proposed fix | Status |
|---|---|---|---|---|---|
| COMP-16 | P3 | design | `.v2-hover-accent`'s colour half has never fired on this screen (drawer ✕, test… | `theme.css:129` → `color: var(--text) !important;`. **Not applied**: `theme.css` is shared by all nine screen… | needs decision: add `!important` to `.v2-hover-accent:hover`'s colour… |
| COMP-17 | P3 | design | Hover taxonomy differs from the design on six controls | — | needs decision: keep code (unified accent hovers across v2) or match … |
| COMP-18 | P3 | functional | A never-scraped active company reads `healthy · scraped never` with a **green**… | a fifth branch before the `active` one: `not scraped yet` with the `--edge` (or `--muted`) dot. | needs decision |
| COMP-19 | P3 | functional | `last_run_warning` is returned but never rendered — one zero-result run looks p… | a `--warn` dot with `last run found nothing` between the `last_error` and `downMap` branches. | needs decision |
| COMP-20 | P3 | design | Drawer is 720 px; the design draws 520 px | — | needs decision: keep 720 (the two-column tuning grid is roomier) or m… |
| COMP-21 | P3 | design | Column widths diverge from the design; `Company` is the column that pays | — | needs decision |
| COMP-22 | P3 | design | The design's "{n} of {N} shown" counter is not built | — | needs decision |
| COMP-23 | P3 | design | The test modal computes per-state row tints and never applies them | either apply `st.bg` (mapped to `--surface` / `--bad-faint` / `--recessed`) or delete the field. | needs decision |
| COMP-24 | P3 | functional | Globally-excluded rows are indistinguishable from per-company exclusions, and t… | a third tag (`Global`) or a `[Global]` prefix strip + warn tint, and add "22 pass this company's filters · 10… | needs decision |
| COMP-25 | P3 | design | The column-header rule is in the wrong place and the wrong weight | — | needs decision |
| COMP-26 | P3 | functional | No in-progress state for the test scrape, and the result table renders every row | — | needs decision |
| COMP-27 | P3 | functional | Add-modal `Save` stays live while `saving` — a double click double-POSTs | `if (saving) return` at the top of `save`. | needs decision (2-line change) |
| COMP-28 | P3 | design | Native `confirm`/`alert` on a screen with no other native dialogs | — | needs decision |
| COMP-29 | P3 | design | No first-run empty state, and the tier-empty copy doesn't name the tiers | — | needs decision |
| COMP-30 | P4 | design | `Ø Fit` uses U+00D8 (Latin O with stroke); the design uses U+2300 `⌀` (diameter… | — | needs decision |
| COMP-31 | P4 | design | Tier chip count is bare, and the tooltip doesn't say what the click will do | — | needs decision |
| COMP-32 | P4 | design | Drawer subtitle is not pluralised | — | needs decision |
| COMP-33 | P4 | design | Row tooltips drop the information the design put in them | — | needs decision |
| COMP-34 | P4 | design | Add-modal control radii/heights differ from the design | — | needs decision |
| COMP-35 | P4 | functional | Dead code | — | fixable, unambiguous — but five separate deletions across the file; l… |
| COMP-36 | P4 | design | The sort menu has six options; the design has five | — | needs decision |
| COMP-37 | P4 | design | `Escape` discards an edited draft with no confirmation, and clicking another ro… | — | needs decision |

## cover-letters (20)

| ID | Sev | Kind | Title | Proposed fix | Status |
|---|---|---|---|---|---|
| CL-08 | P3 | functional | `.v2-menuitem` hover is dead in every popover that sets an inline background — … | Either drop the inline `background: 'transparent'` on the unselected branch (keep only the selected `--accent… | needs decision — `!important` is a one-word change but `.v2-menuitem`… |
| CL-11 | P3 | design | The Download PDF button is pushed off-screen below ~1090 px viewport width | `flexWrap: 'wrap'` on the toolbar, or shrink the Template control (`minWidth: 0` + ellipsis on the template n… | needs decision: which of the two (the design has no narrow spec). |
| CL-12 | P3 | design | The generate panel's explanatory line from the design is missing | Restore the line under the button. | needs decision: keep the leaner panel, or match the design? |
| CL-13 | P3 | functional | No "no voice presets" state — an invisible 0 px gap, and `voice: ""` is posted | Empty copy, e.g. "No voice presets — add them in Settings → AI", and hide the label when the list is empty. | needs decision. |
| CL-14 | P3 | functional | Regenerate failures are reported in the top bar, behind the modal's scrim | Render `err` inside the modal footer while `regenOpen`. | needs decision. |
| CL-15 | P3 | functional | Load-error page has no back link, no retry, and no distinction between 404, 500… | Keep the top bar (or add a "‹ Back to cover letters" link) in the error branch; distinguish 404 ("This letter… | needs decision. |
| CL-16 | P3 | functional | Backend: a non-UUID cover-letter id returns 500 instead of 404 | Parse with `uuid.UUID(cl_id)` at the top of each handler and raise 404 on `ValueError` (or a shared `_get_or_… | needs decision — five handlers, needs a backend restart, and the same… |
| CL-17 | P3 | functional | Rail badge `Cover Letters · N` never refreshes after a generate or a delete | Shell-level refresh (event or a light poll) after mutations. Cross-screen — the same badge is fed by every li… | needs decision (shell-wide, out of this screen's scope). |
| CL-18 | P3 | functional | Neither screen imports `Toast.jsx`; every success is silent and every failure i… | Route the four editor failures and the list generate failure through the toast system; add an `ok` toast for … | needs decision: is inline-only feedback the accepted pattern for thes… |
| CL-19 | P3 | functional | A failed PDF render is invisible | A "Preview failed — showing the last render" line in the toolbar. | needs decision. |
| CL-20 | P3 | functional | A Regenerate started in the editor shows on the list as an unlabelled pending r… | Ignore runs whose `scope_key` is not of the `cl:{resume}:{job}` shape, or have the backend tag the run kind. | needs decision (the shape test is a bit of a heuristic). |
| CL-21 | P3 | functional | Neither screen is keyboard-operable and no control shows focus | At minimum a visible focus style on inputs/textarea (`:focus { border-color: var(--accent); box-shadow: 0 0 0… | needs decision (pattern is v2-wide, not specific to this screen). |
| CL-22 | P4 | functional | Regenerate keeps no lineage — the previous draft is unrecoverable | Either write `parent_id` and keep the superseded row, or drop the column from the API dict so nothing implies… | needs decision. |
| CL-23 | P4 | functional | Header/gutter counts and the archive-band count disagree while a search is typed | Show `N of M` in the header while filtering, and drive the band's chevron from `archOpen` with the search ove… | needs decision. |
| CL-24 | P4 | design | Design deviations (deliberate-looking; listed for the record) | — | needs decision: keep code (consistency) or match design? |
| CL-25 | P4 | functional | Escape closes the Regenerate modal mid-run while Cancel and the scrim refuse | `if (e.key === 'Escape') { onDoc(); if (!regening) setRegenOpen(false) }`. | needs decision (trivial, but "Escape always closes" may be the intend… |
| CL-26 | P4 | functional | The two screens format the same timestamp differently | One shared helper. | needs decision. |
| CL-27 | P4 | functional | `POST /cover-letters` returns a context-free row | `ctx=_build_ctx([cl], db).get(cl.id)` as in the other handlers. | needs decision (backend, restart required). |
| CL-28 | P4 | functional | One shared `err` slot in the editor, cleared only by a successful save | Per-action messages, or clear on a timer for the non-save ones. | needs decision. |
| CL-29 | P4 | design | Disabled arrows still take their hover (matches the design — logged so it is no… | — | needs decision (affordance vs fidelity); no change made. |

## feed (27)

| ID | Sev | Kind | Title | Proposed fix | Status |
|---|---|---|---|---|---|
| FEED-12 | P3 | design | The row action rail's hovers are half dead | add `!important` to the `color`/`background` in `.v2-rail-save/.v2-rail-skip/.v2-rail-copy`, | needs decision |
| FEED-13 | P3 | design | The row SCORE button's hover is the wrong hover | — | needs decision |
| FEED-14 | P3 | design | Detail chevron and "+ Rescore" get a background wash instead of the design's co… | — | needs decision (unified accent hovers are a stated cross-screen decis… |
| FEED-15 | P3 | design | Score/Salary preset pills and every bulk-bar button have no hover | `.v2-bdc` / `.v2-bd` already exist in `theme.css:157-160` for exactly this. | needs decision |
| FEED-16 | P3 | functional | `Escape` closes nothing | one `if (e.key === 'Escape')` branch closing `menu`, `rowMenu`, `headMenu`, | needs decision |
| FEED-17 | P3 | functional | The keyboard legend and the handler disagree | handle `o`, `t`, `c` (three `case` lines), and list `f`/`g`. | needs decision |
| FEED-18 | P3 | design | The collapsed report band mixes the best report's identity with the active tab'… | drive the whole band header off `active`, or hide the counts while the band is open. | needs decision: keep design parity, or make the band self-consistent? |
| FEED-19 | P3 | design | Rescoring an already-scored job gives no signal in the detail panel | `const dScored = reports.length > 0 && !running`, and mirror the `in_flight` update onto | needs decision |
| FEED-20 | P3 | functional | Bulk Save / Skip are silent and leave the header counts stale | — | needs decision |
| FEED-21 | P3 | functional | "Run scoring" with nothing selected is a silent no-op | disable pointer events, or surface "Pick at least one résumé". | needs decision |
| FEED-22 | P3 | design | The live iframe mounts before the frame-check answers, so blocked postings thro… | render the "probing" placeholder (or nothing) while `frameOk === null`. | needs decision |
| FEED-23 | P3 | design | Requirement table: unmet rows aren't tinted, header isn't sticky | — | needs decision |
| FEED-24 | P3 | design | Empty state is one bare line where the design has a guided one | — | needs decision |
| FEED-25 | P3 | design | Title search has no clear affordance | — | needs decision |
| FEED-26 | P3 | design | Source and H-1B dropdowns show no per-value counts | — | needs decision |
| FEED-27 | P3 | design | Score ring geometry and colour scale differ from the design | — | needs decision: keep the heavier ring + traffic-light scale (cross-sc… |
| FEED-28 | P3 | design | Sort's fourth option differs from the design | — | needs decision |
| FEED-29 | P4 | design | Save gives no feedback; the design gives it an undo toast | — | needs decision |
| FEED-30 | P4 | design | Row metrics drift from the design | — | needs decision (the 24 px left inset is consistent across the screen,… |
| FEED-31 | P4 | design | Dead code | — | needs decision (safe deletions, but out of a testing pass's remit) |
| FEED-32 | P4 | functional | The Company menu remembers the previous query | — | needs decision |
| FEED-33 | P4 | functional | The Score and Salary number inputs fire one request per keystroke | — | needs decision |
| FEED-34 | P4 | functional | A job with no company still offers "Ignore everywhere" | hide the item when `!job.company`; skip the leading `·` in the eyebrow. | needs decision |
| FEED-35 | P4 | design | The posting pane is an iframe where the design is a reader column | — | needs decision (this is the v2 iframe direction, not an accident) |
| FEED-36 | P4 | functional | `--iframe-bg` is the same white in both themes | — | needs decision |
| FEED-37 | P4 | design | Palette values differ from the design's map | — | needs decision |
| FEED-38 | P4 | design | No load-more or end-of-list indicator | — | needs decision |

## persona-stats (33)

| ID | Sev | Kind | Title | Proposed fix | Status |
|---|---|---|---|---|---|
| PERS-09 | P3 | design | Remove ✕ hovers to a red *background*; the design specifies a red *glyph* | — | needs decision: keep code (background wash is the v2-wide idiom) or m… |
| PERS-10 | P3 | design | Two collapsible headers on the same screen wash to different colours, and only … | — | needs decision: keep code (`.v2-hover-accent` is shared with other sc… |
| PERS-12 | P3 | design | Picker menu-item hover is dead in the served bundle | — | already fixed in source by this wave's `theme.css:149` `!important` c… |
| PERS-13 | P3 | functional | Every destructive control is one unconfirmed click and PATCHes immediately | — | needs decision: confirm-on-remove for `Remove role` only, or an undo … |
| PERS-14 | P3 | functional | Garbage `qa_bank` entries become blank editable pairs and are counted as answers | — | needs decision: skip unusable entries (and stop counting them), or ke… |
| PERS-15 | P3 | functional | No keyboard access and no focus styling anywhere on the screen | — | needs decision: is v2 taking on a keyboard/focus pass at all? (Same s… |
| PERS-16 | P4 | design | Both column heads are missing the design's right-hand caption | — | needs decision. |
| PERS-17 | P4 | design | The four autofill group headers are missing the design's subtitle | — | needs decision. |
| PERS-18 | P4 | design | `＋ Add answer` carries a hover class that can never do anything | — | needs decision: drop the class, or give the control a real hover (e.g… |
| PERS-19 | P4 | design | `+ Add bullet` is muted grey while every other add-control on the screen is acc… | — | needs decision: align `+ Add bullet` to accent + `.v2-dashadd`? |
| PERS-20 | P4 | design | `--edge` used as small text sits under the AA contrast floor | — | needs decision: keep design fidelity, or lift these three to `--muted… |
| PERS-21 | P4 | functional | Blank Q&A pairs and empty strings are persisted | — | needs decision: filter blanks out of `writeQa`, and/or delete on empt… |
| PERS-22 | P4 | design | Q&A subtitle is truncated relative to the design | — | needs decision. |
| PERS-23 | P4 | functional | "Saves automatically" / "saves on blur" both misdescribe a 500 ms debounce-on-c… | — | needs decision: reword the header, fix CLAUDE.md, or both. |
| STAT-04 | P3 | functional | The Schedules table overflows its card below ~1100 px | Wrap the schedules body in `overflow-x: auto`, or make `Job`/`Job ID` shrinkable. | needs decision. |
| STAT-05 | P3 | design | Two of the four funnel bars use different tokens from the design | — | needs decision: keep code (stage colours shared with Applications) or… |
| STAT-06 | P3 | design | The funnel is upside down, because `Saved` counts jobs and the other three coun… | Drop `Saved` from the funnel, or normalise against `Applied` and label `Saved` as a separate reference row. | needs decision. |
| STAT-07 | P3 | design | The `applied` line uses `--warn` where the design *and theme.css's own comment*… | — | needs decision: (a) switch the stroke to `--gold`; (b) is the `applie… |
| STAT-08 | P3 | design | Activity `Type` menu-item hover is dead in the served bundle | — | already fixed in source (`theme.css:149` gained `!important` this wav… |
| STAT-09 | P3 | functional | The funnel silently falls back to the status snapshot while still claiming "eve… | — | needs decision: surface the fallback in the footnote, or drop the fal… |
| STAT-10 | P4 | design | Score bucket `61-80` uses `--funnel-mid` where the design uses `--funnel-low` | — | needs decision. |
| STAT-11 | P4 | design | The funnel rows lost the design's per-row percentage, and the card lost its sub… | — | needs decision. |
| STAT-12 | P4 | functional | `Avg / call` renders `$0.0000` for a genuinely non-zero average | Fall back to significant digits or `<$0.0001` below the four-decimal floor. | needs decision. |
| STAT-13 | P4 | functional | A long-running job reads `Running · 3671s` | — | needs decision — a one-word change (`dur(j.running?.elapsed_seconds)`… |
| STAT-14 | P4 | design | The Schedules subtitle states the timezone twice and can contradict itself | — | needs decision: suppress the second clause when `TZ_SHORT === 'UTC'`. |
| STAT-15 | P4 | functional | The 30-day series keys on UTC dates while the backend groups on local dates | — | needs decision — use local date parts instead of `toISOString`. |
| STAT-16 | P4 | functional | Nothing on the screen is a link, including the one the rail advertises | — | needs decision — at minimum give the Run-history card an id and scrol… |
| STAT-17 | P4 | design | Hover, column and label deviations from the design (grouped) | — | needs decision (per the addenda, most of these look like deliberate c… |
| STAT-18 | P4 | functional | Both logs are silently truncated | — | needs decision. |
| STAT-19 | P4 | functional | Sankey and funnel disagree on how many applications there are | — | needs decision. |
| STAT-20 | P4 | design | KPI tile internals differ from the design | — | needs decision. |
| STAT-21 | P4 | functional | Border tokens used as fills and as text lose their contrast in dark | — | needs decision: use `--line-strong` in dark, or accept it as an inten… |
| STAT-22 | P4 | design | No keyboard access on Stats either | — | needs decision — same call as PERS-15. |

## resumes (20)

| ID | Sev | Kind | Title | Proposed fix | Status |
|---|---|---|---|---|---|
| RES-11 | P3 | functional | Editor: section cards, bullet rows, modal rows all fractional | integer line-heights on the two 10.5 px labels (16 px) and on the modal rows; for the bullet textareas, eithe… | needs decision: override the deliberate 1.5 bullet line-height, or ac… |
| RES-13 | P3 | functional | "+ N more ›" promises N and delivers a first-word search over everything | make the affordance expand the card in place (show all `copies`), or seed the search with a filter that scope… | needs decision |
| RES-14 | P3 | functional | A 200-character base name is not truncated and overflows its card | give the name span `flex: '0 1 auto'; minWidth: 0; whiteSpace: 'nowrap'; overflow: 'hidden'; textOverflow: 'e… | needs decision (trivial, but it changes the card's headline layout) |
| RES-15 | P3 | functional | Escape closes none of the four modals | one `useEffect` keydown listener per modal (or a shared `useEscape(onClose)`). | needs decision |
| RES-16 | P3 | functional | Destructive edits have no confirm and no undo; the one confirm is the browser's | an `undo` toast for role/education/project/publication removal (the state is already a deep clone, so the sna… | needs decision |
| RES-17 | P3 | design | Disabled primary buttons are filled with `--edge`, which reads as enabled | `background: 'var(--line)'`, `color: 'var(--muted)'` when disabled. | needs decision: keep code (consistency with other v2 disabled pills) … |
| RES-18 | P3 | functional | Import PDF shows its busy state on the *other* button | separate `busy` per action, label the Import button "Parsing…", and disable both while either runs. | needs decision |
| RES-19 | P3 | functional | The archived band and the "+N more" search render every row unvirtualised | cap the archived list (e.g. 100 + "show more"), or paginate server-side. | needs decision |
| RES-20 | P3 | functional | On a job-less copy the "one next step" is an action that can only fail | when `!doc.job_id`, skip the Score stage (go straight to Cover letter) and say so in the sub-line; render a d… | needs decision |
| RES-21 | P3 | functional | A missing, malformed or just-deleted id redirects silently | (frontend) push an `error` toast "That résumé no longer exists." before navigating. | backend half **fixed + verified**; frontend redirect needs decision |
| RES-22 | P3 | design | Design deviations (grouped — all read as decisions) | — | needs decision: keep code (consistency) or match design? |
| RES-23 | P4 | functional | `.v2-navlink:hover`'s colour rule never fires where the caller sets an inline c… | — | needs decision: add `!important` to `.v2-navlink:hover`, or accept ba… |
| RES-24 | P4 | functional | One localStorage key holds the section-open state for every résumé and for Pers… | — | needs decision (arguably the desired behaviour — noting it because th… |
| RES-25 | P4 | functional | The success toast that carries "Open ↗" expires in 2.5 s | — | needs decision: give action-bearing success toasts a longer TTL (or `… |
| RES-26 | P4 | functional | A freeform tailor never reports completion | — | needs decision |
| RES-27 | P4 | functional | `setSearchParams` is declared and never used; `?job=` is inert | — | needs decision: drop the dead declaration, or wire `?job=` to presele… |
| RES-28 | P4 | functional | Dead code and always-false branches | — | needs decision |
| RES-29 | P4 | functional | Archived view has no empty branch; the archived sort comment is wrong | — | needs decision |
| RES-30 | P4 | functional | The score poll resolves on any numeric `Tailored`, so a re-score reports the ol… | capture the pre-run value and resolve only when it changes, or key the poll on `/monitor/active` (scope `f"{j… | needs decision |
| RES-31 | P4 | design | "autosaves on blur" is wrong — saving is per keystroke, debounced 500 ms | — | needs decision: reword to "autosaves"? |

## searches (22)

| ID | Sev | Kind | Title | Proposed fix | Status |
|---|---|---|---|---|---|
| SRCH-08 | P3 | functional | Test-modal table rows land on `.75` px | Integer line-heights on the params strip (17px), the footer summary (17px) and the modal title (26px), as don… | needs decision: fix now (touches four more inline styles inside the m… |
| SRCH-09 | P3 | functional | Header “N need attention” and the rail's “N sources need attention” count diffe… | Either narrow the header to `downMap` only, or label it distinctly (e.g. “1 search needs attention” vs the ra… | needs decision: keep the broader per-screen predicate (it surfaces a … |
| SRCH-10 | P3 | functional | The rail “Searches” badge diverges from the header count after create / duplica… | Have the shell expose a `refreshCounts()` via context (or an outlet context / custom event) that the screen c… | needs decision: cross-screen change (Companies, Résumés, Cover Letter… |
| SRCH-11 | P3 | functional | A legacy `url`-mode search shows a Mode dropdown reading “Keyword (JobSpy)” | Push a disabled `['url', 'Direct URL (legacy)']` option into `MODE_OPTIONS` when `d.search_mode === 'url'`. | needs decision: is a legacy-`url` row still expected in this database… |
| SRCH-12 | P3 | functional | `toPayload` silently rewrites three fields the user just cleared | `?? ` instead of `//` for the numerics, and only default Location for keyword mode. | needs decision: is “0 hours old” a meaningful configuration, or is th… |
| SRCH-13 | P3 | design | Test modal is 980px wide; the design says 880px | — | needs decision: keep 980 (more room for the 7-column table) or match … |
| SRCH-14 | P3 | design | Test-modal Salary column is 120px / 9.5px; the design says 92px / 10.5px | — | needs decision: the wider/smaller salary cell fits `$190,000 – $240,0… |
| SRCH-15 | P3 | design | New-search card body and edit drawer paint `--bg`; the design specifies `#fdfcf… | — | needs decision: the set-back form panel is exactly the case `--recess… |
| SRCH-16 | P3 | design | Header rule is full-bleed and the left gutter is 24px; the design insets the ru… | — | needs decision: the 24px left gutter looks like a deliberate cross-sc… |
| SRCH-17 | P4 | design | Hovers present in code that the design does not author | — | needs decision: the card hover is documented in-code as “same card ho… |
| SRCH-18 | P4 | design | Card action cluster deviates from the design in five small ways | — | needs decision: all five read as deliberate alignment work — confirm,… |
| SRCH-19 | P4 | functional | `until()` has no day unit, so a two-day-out sweep reads “next scheduled run in … | — | needs decision: add `Xd Yh` above 24h? |
| SRCH-20 | P4 | functional | “last run 3d ago” and “next scheduled run in …” freeze until an unrelated re-re… | — |  |
| SRCH-21 | P4 | functional | Escape closes the ⋯ menu and the test modal, but not the edit drawer or the New… | — |  |
| SRCH-22 | P4 | functional | New-card Cancel keeps the typed draft | — |  |
| SRCH-23 | P4 | functional | No concurrency guard on Test | — | read from source; not reproduced live (would need two concurrent real… |
| SRCH-24 | P4 | functional | `/health/entities` and `/scheduler/jobs` are fetched once and never refreshed | — |  |
| SRCH-25 | P4 | functional | The depth indicator advertises `cursor: help` but its click opens the editor | — |  |
| SRCH-26 | P3 | functional | The backend does not enforce the extension-search gating the UI applies | 404/409 on `/run` and `DELETE` when `search_mode` is an extension mode. | needs decision: add a server-side guard (backend restart required), o… |
| SRCH-27 | P4 | functional | A preview row with a null `url` renders as a styled but dead link | — |  |
| SRCH-28 | P4 | functional | The run poll tears itself down and rebuilds every 3s, and keys on any job's `sc… | — |  |
| SRCH-29 | P4 | design | No pending state on “Create search” or “Save changes” | — |  |

## settings (17)

| ID | Sev | Kind | Title | Proposed fix | Status |
|---|---|---|---|---|---|
| SET-08 | P3 | functional | Optimistic state is never rolled back after a failed save | Capture the previous value in `save()` and restore it in the catch; the `llm` override toggle needs its `ovr`… | needs decision: roll back optimistically-applied values on failure, o… |
| SET-11 | P3 | design | Four rows overflow their control column at 1024 px | Make the label column `flex: 0 1 340px` (or stack the row below ~1150 px). | needs decision: narrow-viewport support was never in scope for the de… |
| SET-12 | P3 | functional | No keyboard operability, no labels, no ARIA | `role="button"` + `tabIndex={0}` + Enter/Space on the shared primitives (`Select`, `Toggle`, `ActionBtn`, the… | needs decision: cross-screen — the same primitives are used on Feed/C… |
| SET-13 | P3 | design | `--edge` body text falls below 4.5:1 in both themes | — | needs decision: cross-screen token question (`--edge` as text vs as a… |
| SET-14 | P3 | design | Dark mode: the toggle knob is white on the light-green accent — 2.16:1 | Give `--knob` a dark value (`--surface` / `#28251b`), or use `--accent-ink` for the knob when the switch is o… | needs decision: token-level, affects every v2 switch. |
| SET-15 | P3 | design | Model-catalog `×` hover changes colour only; the design changes the border too | A `.v2-hover-bad-bdc` rule (border + colour); adding `border-color` to the existing class would change the `×… | needs decision (I left `theme.css` alone — another agent committed ho… |
| SET-16 | P3 | design | Catalog typeahead is a plain inline list, not the design's dropdown | — | needs decision: keep the simpler inline list, or build the design's d… |
| SET-17 | P3 | design | Catalog offers `×` on seeded models; the design restricts removal to your own a… | — | needs decision: keep code (removal of seeded entries is useful) or ma… |
| SET-18 | P3 | design | Advanced is missing the design's H-1B refresh and Job cleanup triggers | — | needs decision: add the two rows back, or is Stats › Run history the … |
| SET-20 | P4 | design | Every numeric/cron box is 135 px against the design's 170 px; four Selects are … | — | needs decision: keep the tighter code widths (consistent 135/260 acro… |
| SET-21 | P4 | design | The Edit modal is 1020 px wide against the design's 680 | — | needs decision: keep code (long prompts need the room) or match desig… |
| SET-22 | P4 | design | The webhook-secret preview isn't in a box | — | needs decision (design fidelity vs the code's "preview + button" patt… |
| SET-23 | P4 | functional | An empty model catalog opens a 12 px empty popover | Render a muted "no models for this provider — add one under Model catalog" row instead of an empty menu. | needs decision. |
| SET-24 | P4 | design | Section subtitles and two row labels drifted from the design | — | needs decision: adopt the design's copy where the code is not more co… |
| SET-25 | P4 | functional | The Edit modal drops keystrokes typed within 600 ms of closing | Flush the pending commit in `onClose` (or on the Done click) before unmounting. | needs decision: flush-on-close, or is the 600 ms window acceptable? |
| SET-26 | P4 | functional | A leftover scratch résumé `ZZTEST Base A` is in the Default-résumé dropdown | — | needs decision: whoever owns the Résumés pass should remove it. |
| SET-27 | P3 | design | PATCH `warnings` are discarded, so a scheduler / semaphore / dedup-reload failu… | Flash `warnings[0]` as `bad` when the array is non-empty; add `inputMode="numeric"` plus a digit filter to th… | needs decision: surface `warnings`, and how strict should client-side… |

## shell (4)

| ID | Sev | Kind | Title | Proposed fix | Status |
|---|---|---|---|---|---|
| SHELL-02 | P3 | functional | Theme toggle is two-state; design cycles Light → Dark → System | One theme store (`light/dark/system`) + `prefers-color-scheme` listener + no-flash boot script; out of scope … | needs decision: schedule with the theming groundwork, or accept two-s… |
| SHELL-04 | P4 | design | Unauthored hovers on the Welcome modal | — | needs decision: keep (consistency with the app's link affordance) or … |
| SHELL-05 | P4 | design | Rail values that differ from the Nav Rail board | — | needs decision: these look like deliberate consistency/contrast chang… |
| SHELL-06 | P4 | design | `App.jsx` and V2App keep separate copies of the theme flag | — | fold into SHELL-02. |

## FINDINGS (3)

| ID | Sev | Kind | Title | Proposed fix | Status |
|---|---|---|---|---|---|
| F-003 | P4 | functional | theme.css · 10 tokens defined but never used | Delete the 9 alias lines and `--paper`, or wire `--paper` to the PDF-preview iframe ground (`--iframe-bg` alr… | needs decision: delete now, or keep until the primitive layer lands? |
| F-004 | P4 | design | theme.css · 5 shadow tokens have no dark variant | Add dark overrides with higher alpha (e.g. `.28 → .55`, `.16 → .4`) and verify by measuring the pixel delta a… | needs decision (design choice — I can propose values and measure). |
| F-009 | P3 | functional | Cross-cutting · Half-pixel rows persist outside the rows the screen passes fixed | One systematic pass: a small set of `.v2-t13/.v2-t12/.v2-t11` utility classes (or inline integer line-heights… | systematic pass done (73 one-line line-heights, `stage3/F-009-linheig… |

## cross-cutting (1)

| ID | Sev | Kind | Title | Proposed fix | Status |
|---|---|---|---|---|---|
| X-01 | P4 | functional | `/api/monitor/active` entries carry no target id for company scrapes | Populate `target_id` with the company id in `launch_background` for company scrapes (résumé jobs already carr… | needs decision. |
