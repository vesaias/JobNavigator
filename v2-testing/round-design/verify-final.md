# Design-pass fixes — live verification

Branch `v2-redesign` @ HEAD `a781b7ac8766dbc169558dde076f3d62de65707e`, built and live (frontend + backend containers restarted 2026-09-04 00:08 UTC, ~24 min before this pass started). Read-only pass: no commits, no builds, no restarts, no source edits. Playwright inside the backend container against `http://caddy`, API key `pick-a-password`, harness `/tmp/v2t/h.py` (pre-existing from the design-pass sessions), scripts written locally then `docker compose cp` + `docker compose exec -T backend python3 /tmp/v2t/<name>.py`. `DOCKER="/c/Program Files/Docker/Docker/resources/bin/docker.exe"; export MSYS_NO_PATHCONV=1`. Theme/skin set via localStorage `jobnavigator_theme` / `jobnavigator_skin` in the harness's `extra_ls=` init-script param before `go()`. No real LLM calls: item 6 stubbed `POST /api/resumes/tailor` + `GET /api/monitor/*`; all other items either used real, cheap/free endpoints (dry-run test scrapes, DB reads) or pure-Python unit calls.

Read first: `v2-testing/round-design/smoke-final.md` (Fixes section, DS-S-31/11/12/21/32), `flows-A-final.md` (DS-A-02/03 + its Fixes section), `flows-B-final.md` (DS-B-01/02/03 + its Fixes section), `v2-testing/round3/verify-final.md` (harness method precedent).

Scratch data: item 4 created two `ZZV Verify App @ ZZV Verify Co` applications (to exercise the save→Escape flow) — both deleted, plus the auto-created `ZZV Verify Co` company and its 2 jobs, swept at the end. No other item wrote any DB row (items 1/3/6/7/8/9/10 are pure reads/stubs; item 2 reads only; item 5 is a real dry-run "Test" that writes nothing by design).

---

## Results

| id | status | measured |
|---|---|---|
| 1. DS-S-31 (alt skin in light mode) | ✔ | `.jn-v2` `--accent`: light/default `#3f6b52` vs light/alt `#3f52a8` (indigo) — differ as expected. dark/default `#8dbb9f` vs dark/alt `#9aabee` — differ. Page background (`getComputedStyle(.jn-v2).backgroundColor`): light/default `rgb(252,251,247)` vs light/alt `rgb(247,248,250)` — differ; dark/default `rgb(30,28,23)` vs dark/alt `rgb(22,26,33)` — differ. Direct `document.styleSheets[…].cssRules` scan (light+alt loaded) found **both** `.jn-v2[data-skin="alt"]` (standalone) **and** `.jn-v2[data-skin="alt"][data-theme="dark"]` as parsed rules — the previously-missing standalone rule is present in the live parsed stylesheet. The `theme.css:318` comment-terminator fix is live and working in all 4 combinations. |
| 2. DS-S-11/12 (shelf header clipping) | ✔ | `/v2/resumes`: "+ New résumé" button at 1440 → `left=1283, right=1410` (≤1440 ✔), text reads the full `"+ New résumé"` (not `"+ N"`); at 1024 → `right=994` (≤1024 ✔), same full text. Search field kept its full declared width both times: `300px` (1440: `right=1273`; 1024: `right=857`). `document.documentElement.scrollWidth` = `innerWidth` at both sizes (1440/1440, 1024/1024). `/v2/cover-letters`: no `"+ New letter"` button exists on this shelf (confirmed unchanged from the design-pass docs — "Generate new" is an always-open inline panel, not a button, so there is nothing to clip); its search field is `280px` wide, right edge `1410` (1440) / `994` (1024) — well inside the viewport, `scrollWidth == innerWidth` at both sizes. The shared `SearchInput` wrapper-width fix (`ui.jsx:295`) is live on both shelves. |
| 3. Select Escape (Settings, two-stage) | ✔ | No modal open: opened the Theme `Select` (`aria-label="Theme"`) → 1 `[role="listbox"]`; **1 Escape** → 0 listboxes (closes on its own, nothing else to close). Scanned all 19 `Edit` modals on `/v2/settings` (rubric/prompt/JSON/list editors) — none contain a `Select`; found the **Model catalog** modal (`Manage…` button) does contain one (`OpenRouter▾`, `aria-haspopup="listbox"`). With that modal open: opened its Select → 1 listbox, modal still present (1). **1st Escape** → listbox 0, modal still 1 (options closed, modal untouched). **2nd Escape** → modal 0 (now closes). Separately, with the modal open and **no** Select opened, a **single** Escape closed the modal directly (0). Matches the documented `useEscape` bubble-phase / `Select` capture-phase Escape ordering (`ui.jsx:311-336`) exactly. |
| 4. DS-A-03 (Applications post-save Escape) | ✔ | Opened `+ Log application`, filled Title/Company/`Posting URL` (aria-labeled fields, not the page search box), `Save application` → 200, drawer opened for the new application. **1 Escape** immediately after save → `document.body.innerText` does **not** contain `"Discard this application?"`, and `document.elementFromPoint(50,50)` still resolves to a normal page element (no scrim intercepting clicks). Re-opened `+ Log application`, typed a title only (no save) → **1 Escape** → `"Discard this application?"` **is** present (dirty-draft case correctly still guarded). **2nd Escape** → dialog text gone. Console: 0 errors, 0 page errors throughout. Matches the `Applications.jsx:158`/`:489` fix (`closeLog()` gated on `logOpen`, `onSaved` clears `logDirty`). Cleanup: both created applications deleted (`DELETE /applications/{id}` → 200 each), the auto-created `ZZV Verify Co` company deleted (`DELETE /companies/{id}` → 200), its 2 auto-created jobs removed by scoped SQL (no job-delete endpoint) — final DB check found 0 `ZZV` rows anywhere and `applications` back to 377. |
| 5. DS-A-02 (Searches test applies global exclude) | ✔ | `title_exclude_global` setting already populated (84 keywords incl. `marketing`, `security`, `automation`, …) — no patch needed. Real `POST /api/searches/34463384-4fd2-4168-b412-06343de9729c/test` (the "JobSpy" keyword search, `search_term="product manager"`) → 200 in ~24s, response carries **`global_excluded_count=14`**, **`global_exclude_keyword_count=84`**, `after_search_filter=88` (vs `after_filter=66` post-global), and 11+ rows with `global_excluded_by` populated when the title hit a global keyword, e.g. `"Product Manager, HIV Treatment Marketing"` → `global_excluded_by:["marketing"]`, `reason:"[Global] Excluded by: marketing"`; `"Senior Product Manager - Reconciliation"` → `["reconciliation"]`. Live UI check (clicked the real `⚗ Test` icon on the JobSpy card at `/v2/searches`, real dry-run, ~24s — the frontend's own 30s client timeout made one earlier attempt race and fail, a pre-existing/unrelated axios-timeout artifact, not a DS-A-02 regression; a retry completed within budget): footer read `"71 kept · 9 title-filtered · 12 removed by the global list (91 pass this search's filters) · 8 would be ignored (body phrases) · 100 raw · 24.3s · 42 not body-checked…"`, and the page text contains the `GLOBAL` badge. Matches `routes_searches.py:370-394` + `Searches.jsx:745-750,884-890,866`. |
| 6. DS-B-02 (background poller reads real run status) | ✔ (with one clarification) | Stubbed `POST /api/resumes/tailor` → 202 `{run_id:'zzv-run', status:'running', chain_score:'light'}`, `GET /api/monitor/active*` → `[]`, `GET /api/monitor/history*` → `[]`. **Case A** — `GET /api/monitor/run/zzv-run` → 200 `{status:'failed', error:'boom'}`: on the base `PM` résumé (`22ce0e5b…`), opened "✦ Tailor for a job…", pasted a freeform JD, clicked "✦ Tailor" → after the run vanished from `/monitor/active`, the toast read exactly **`"! Tailoring failed — boom ✕"`**, and no `"is ready"`/`"finished"` success wording appeared anywhere on the page. **Case B** — `GET /api/monitor/run/zzv-run` → 404 **and** `GET /api/monitor/history*` → `[]` (the genuine "run not found anywhere" case): toast read **`"Tailoring finished, but the copy could not be located. Résumés ↗ ✕"`** — the neutral wording, as documented. *Clarification on the task wording*: `status:'completed'` with no copy found (tested separately) does **not** produce the neutral toast — it produces the still-checkmarked **`"✓ Tailoring finished. Résumés ↗"`** success-styled fallback, per the fix's own three-way branch (`failed`→error, `completed`→success incl. its no-copy-found fallback wording, run-not-found→neutral). The neutral "could not be located" toast is reached via the **run-not-found** path (404 + empty history), which is what I verified in Case B and matches `flows-B-final.md`'s own documented repro (`zzbd_04h_falsetoast.py`: "stub … `GET /monitor/run/* → 404` and `/monitor/history → []` … should now produce the neutral 'could not be located' toast"). Both real behaviors match `hooks.js:99-134` (`fetchRunOutcome`/`runFailed`/`runFailureReason`) and `ResumeEditor.jsx:151-173`. Console: only the expected stubbed-404 log entry in Case B, 0 unexpected errors either case. No résumé/copy was actually created (the tailor POST was fully intercepted). |
| 7. DS-B-03 (autofill never returns the JSON envelope) | ✔ | `backend.api.routes_autofill._extract_answer(raw)`, run directly in the container: clean JSON `{"answer": "I led the migration and delivered on time."}` → `"I led the migration and delivered on time."` (no brace). Fenced JSON ```` ```json\n{"answer":"Fenced answer here."}\n``` ```` → `"Fenced answer here."` (no brace). Truncated envelope `{"answer": "At additiv I owned a Tier 1 bank platform delivery where 5 engineeri` (the exact shape that leaked pre-fix) → salvaged to `"At additiv I owned a Tier 1 bank platform delivery where 5 engineering squads had conflicting views"` (no brace). Plain prose → passed through unchanged (no brace). Unsalvageable wrapper `{"foo": "bar"` → `""` (empty — the endpoint raises `ValueError` on this and returns 502, per the fix, rather than serving anything starting with `{`). **All four/five outputs: none start with `{`.** Matches `routes_autofill.py:136-186`. |
| 8. DS-B-01 (disabled/busy Button keeps role) | ✔ (with one nuance) | `/v2/ui`'s **"live busy toggle"** sample (`<Button onClick={…} busy={busy}>` — a real Button with a real handler): before click → `role="button" tabindex="0"`; **300ms into** the 1.6s busy window (`"Working…"`, mid-flight) → the actual `.v2-ctl` button div reads **`role="button" tabindex="-1" aria-busy="true" aria-disabled="true"`**; after the busy window clears → back to `role="button" tabindex="0"`. This is the exact contract `ui.jsx:65-74`'s `act()` restores. *Nuance*: the gallery's two purely-decorative static swatches labeled "PRIMARY · DISABLED / Save" and "PRIMARY · BUSY / Saving…" (and their `secondary` counterparts) are declared with **no `onClick` prop at all** (`<Button disabled>Save</Button>`, `<Button busy>Saving…</Button>` in `UiGallery.jsx:116-121`) — `act(fn, off)` short-circuits to `{}` whenever `fn` is falsy regardless of `off`, so these two specific static samples carry neither `role` nor `tabindex`. This is `act()` behaving exactly as coded (an inert control with no handler was never a real button to begin with), not a regression, but it means those two particular gallery rows don't themselves demonstrate the fix — the disabled `Select`/`MoveArrows`/`IconButton` samples elsewhere on the same page (which do have handlers) do carry `role="button" tabindex="-1" aria-disabled="true"`, confirmed directly. Also cross-confirmed on a real in-app control during item 6: the "✦ Tailor" button, while `aria-disabled="true"` (JD box empty), read `role="button" tabindex="-1"`. |
| 9. Skin proof, fixed-height check | ✔ (1 benign exception, not a defect) | Compared `/tmp/v2t/shots/D7/styles.json` (5644 keys) vs `D7alt/styles.json` (5688 keys), 5640 common keys. 90 elements have a differing `rest.height` between the two skins. Applying the filter (control-class path — `v2-ctl`/`v2-bd`/`v2-bdc`/`v2-act`/`v2-row`/`v2-menuitem`/`input`/`select` — **or** a `lineHeight` that fits the height exactly once in either skin) flagged **2** of the 90 (same DOM element, once per theme: light and dark). Both are the **same** settings-page hint span (`main>div>div>div.v2-scroll:1>div>div:1>div:6>div:0>span:1`, text `"Model that answers appli…"`) — an 11.5px muted-color description label under a model picker, **not** a control-class element. Root cause confirmed directly: `fontFamily` differs (`"Public Sans"` in default vs `Inter, "Source Sans 3"…` in alt) and that's the *only* other property that differs between the two rows besides `color`/`height` — the narrower label wraps to 2 lines (`16px→32px`, `lineHeight` stays `16px` both) under Inter but stays on 1 line under Public Sans. This is the direct, permitted consequence of "switching skin may change colour and font family" (the D6 contract quoted in DS-S-31) reflowing a short hint string — not a control losing/gaining a fixed single-line box. The other 88 of 90 diffs are all confirmed multi-line prose (résumé/persona textareas, long descriptive `<div>`s) growing between skins for the same font-metric reason, exactly as expected. **0 control-class elements** among the 90 diffs. |
| 10. D6b → D7 rail pixel change (179,172,185,286) | ✔ expected (glyph, not colour) | `PIL.ImageChops.difference` on `v2_stats__dark__1440.png` (D6b vs D7) confirms a diff in that region (full-page diff bbox is much larger — unrelated content also changed between crawls — but the named box is inside it). Cropped + 6× upscaled the region for a visual read: D6b shows rail counts **`127`** (Companies) / `377` (Applications, unchanged) / **`5`** (Résumés); D7 shows **`126`** / `377` / **`4`** — i.e. the box spans the right edge of the Companies-count and Résumés-count digit glyphs in the rail, which shifted by one digit's worth of antialiased pixels because the live counts moved (`127→126`, `5→4`) between the two crawls — consistent with the "counts drift mid-run from concurrent background activity" pattern already documented elsewhere in this round's reports (e.g. Feed `22→25`). Live DOM check at `/v2/stats` dark 1440 confirms the box lands exactly on the rail's badge `<span>` elements (`Companies126`, `Applications377`, `Résumés4`) and nothing else. **Confirmed: a rail glyph/count change, not a colour change** — matches the expected, non-defect interpretation. |

## Summary

**10 of 10 ✔.** Every fix listed in `smoke-final.md`/`flows-A-final.md`/`flows-B-final.md`'s "Fixes (source, unbuilt)" sections is confirmed live and working against the built, restarted stack at HEAD `a781b7a`:

- **DS-S-31** (alt skin inert in light mode) — fixed; the standalone `.jn-v2[data-skin="alt"]` rule now parses and applies in all 4 theme×skin combinations.
- **DS-S-11 + DS-S-12** (shelf header CTA clipping / search-input latent overflow) — fixed; both shelves clean at 1440 and 1024, `scrollWidth == innerWidth`, button text intact.
- **DS-S-21 + DS-S-32** (`Select` doesn't close on Escape) — fixed; capture-phase Escape closes the listbox first, leaves a parent modal for a second Escape, and reaches the modal directly with no listbox open.
- **DS-A-03** (post-save Escape wrongly raised the discard dialog) — fixed; verified both the clean (no dialog) and dirty (dialog appears, second Escape closes it) paths.
- **DS-A-02** (Searches test preview silently promised jobs the real run then dropped) — fixed; the preview's `global_excluded_count`/`global_exclude_keyword_count`/`global_excluded_by`/`[Global] Excluded by: …` all present, footer and `GLOBAL` badge live in the UI.
- **DS-B-02** (background poller reported failed runs as success) — fixed for both the `failed` path (named error toast) and the true run-not-found path (neutral toast); noted a wording nuance vs. the task brief's shorthand (see item 6).
- **DS-B-03** (JSON envelope leaking into the autofill answer) — fixed; `_extract_answer` never returns anything starting with `{` across clean/fenced/truncated/prose/unsalvageable inputs.
- **DS-B-01** (disabled/busy control losing its role) — fixed for real (handler-bearing) controls; noted that two purely-decorative static gallery swatches have no handler at all and so don't exercise the fix (not a regression — `act()`'s own documented contract).
- **Skin proof / fixed-height check** — clean; the one exception found is a benign font-family-driven text-wrap of a description label, not a control losing a fixed box.
- **D6b→D7 rail pixel diff** — confirmed a rail count-glyph change (live-data drift between two crawl snapshots), not a colour regression.

No P1/P2 defects found. No behavior contradicted any of the three source documents' "Fixes" sections.

## ZZV sweep

Created only in item 4: 2 applications (`ZZV Verify App @ ZZV Verify Co`), which auto-created 1 company (`ZZV Verify Co`) and 2 jobs. All removed during item 4's own cleanup step (`DELETE /applications/{id}` ×2 → 200, `DELETE /companies/{id}` → 200, scoped SQL `DELETE FROM jobs WHERE company LIKE 'ZZV%'` → 2 rows — no job-delete endpoint exists). Final direct-DB sweep across `jobs`, `companies`, `applications`, `searches`, `resumes`, `cover_letters`: **0 `ZZV` rows anywhere**; `applications` count back to the documented baseline of **377**.

## Settings restore

No setting was modified during this pass. `title_exclude_global` (item 5) was read only — it already had 84 entries, so the "PATCH one keyword in / PATCH it back" contingency in the task brief was not needed. No other `PATCH /api/settings` call was made by any script in this pass.
